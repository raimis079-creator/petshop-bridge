<?php
/** Plugin Name: TEMP PS S1719x3 — snippet 465 iš gz archyvo (ištrintas su neaktyviais): tokenų sha256/tipas/ilgis, read-only */
add_action('wp_loaded', function(){ if(!isset($_GET['ps_s1719x'])) return; $rows=json_decode(gzdecode(file_get_contents('/home/gyvunai2/domains/petshop.lt/ps-archyvas/s1719-db/snippets-inactive-s1719.json.gz')),true); $c=''; foreach($rows as $row){ if((int)$row['id']===465){ $c=$row['code']; break; } } preg_match_all('/gh[pousr]_[A-Za-z0-9]{20,}|github_pat_[A-Za-z0-9_]{20,}/',$c,$m); $out=[]; foreach(array_unique($m[0]) as $v) $out[]=['tipas'=>strpos($v,'github_pat_')===0?'fine-grained':'classic','ilgis'=>strlen($v),'sha256'=>hash('sha256',$v)]; preg_match_all('/[^\n]{0,40}define\([^\n]{0,70}/',$c,$dl); $lines=array_map(function($l){return preg_replace('/gh[pousr]_[A-Za-z0-9]{20,}|github_pat_[A-Za-z0-9_]{20,}/','***',$l);},$dl[0]); header('Content-Type: application/json'); echo json_encode(['v'=>'S1719x3','rasta_465'=>$c!=='','tokenai'=>$out,'define'=>$lines],JSON_UNESCAPED_UNICODE); exit; },1);
