import { execSync } from "child_process";
import fs from "fs";
function putText(n,s){const repo=process.env.GH_REPO,tok=process.env.GH_TOKEN;const url='https://api.github.com/repos/'+repo+'/contents/analize/'+n;let sha='';try{sha=JSON.parse(execSync('curl -s --max-time 30 -H "Authorization: Bearer '+tok+'" "'+url+'?ref=main&t='+Date.now()+'"',{encoding:'utf8'})).sha||'';}catch(e){}const b={message:'x',branch:'main',content:Buffer.from(s,'utf8').toString('base64')};if(sha)b.sha=sha;fs.writeFileSync('/tmp/pf.json',JSON.stringify(b));execSync('curl -s --max-time 40 -X PUT -H "Authorization: Bearer '+tok+'" -d @/tmp/pf.json "'+url+'"',{encoding:'utf8'});}
const BASE='https://dev.avesa.lt';const U=process.env.WP_USER||'';const P=(process.env.WP_APP_PASS||'').replace(/\s+/g,'');
const PHP="if ( ! defined('ABSPATH') ) { return; }\nadd_action('wp_loaded', function(){\n  if ( ! isset($_GET['ps_dash_recon']) ) { return; }\n  if ( ($_GET['token']??'') !== 'cmplz_6680aa2a42151d54fa8d64ec' ) { return; }\n  $out = array();\n  global $wpdb;\n\n  // refill_tracking struktura\n  $t = $wpdb->prefix.'ps_refill_tracking';\n  if($wpdb->get_var(\"SHOW TABLES LIKE '$t'\")){\n    $out['refill_columns'] = $wpdb->get_col(\"SHOW COLUMNS FROM $t\");\n  } else { $out['refill_columns'] = 'NO TABLE'; }\n\n  // reminders struktura\n  $r = $wpdb->prefix.'ps_reminders';\n  if($wpdb->get_var(\"SHOW TABLES LIKE '$r'\")){\n    $out['reminders_columns'] = $wpdb->get_col(\"SHOW COLUMNS FROM $r\");\n  } else { $out['reminders_columns'] = 'NO TABLE'; }\n\n  // Refill klase \u2014 kokie metodai\n  $out['refill_class'] = class_exists('Petshop_Refill_Engine');\n  if(class_exists('Petshop_Refill_Engine')){\n    $out['refill_methods'] = get_class_methods('Petshop_Refill_Engine');\n  }\n  $out['reminders_class'] = class_exists('Petshop_Reminders');\n  if(class_exists('Petshop_Reminders')){\n    $out['reminders_methods'] = get_class_methods('Petshop_Reminders');\n  }\n\n  header('Content-Type: application/json; charset=utf-8');\n  echo wp_json_encode($out, JSON_UNESCAPED_UNICODE|JSON_UNESCAPED_SLASHES|JSON_PRETTY_PRINT);\n  exit;\n}, 6);";
function api(method,path,body){const auth='-u "'+U+':'+P+'"';let cmd;if(body){fs.writeFileSync('/tmp/b.json',JSON.stringify(body));cmd='curl -s -k -A "Mozilla/5.0" --max-time 90 -w "\nHTTP:%{http_code}" '+auth+' -X '+method+' -H "Content-Type: application/json" --data-binary @/tmp/b.json "'+BASE+path+'"';}else{cmd='curl -s -k -A "Mozilla/5.0" --max-time 60 '+auth+' -X '+method+' "'+BASE+path+'"';}let r;try{r=execSync(cmd,{encoding:'utf8',maxBuffer:30000000});}catch(e){r=(e.stdout||'');}return r;}
function sh(c){try{return execSync(c,{encoding:'utf8',maxBuffer:30000000});}catch(e){return (e.stdout||'')+'[ERR]';}}
(async()=>{let id=0;
  const c=api('POST','/wp-json/code-snippets/v1/snippets',{name:'Dash Recon tmp',desc:'x',code:PHP,scope:'global',active:true,priority:10});
  try{id=JSON.parse(c).id;}catch(e){}
  execSync('sleep 2');
  const r=sh('curl -s -k -A "Mozilla/5.0" --max-time 45 "'+BASE+'/?ps_dash_recon=1&token=cmplz_6680aa2a42151d54fa8d64ec"');
  putText('dash_recon.json', r);
  if(id) api('POST','/wp-json/code-snippets/v1/snippets/'+id+'/deactivate',{});
})();
