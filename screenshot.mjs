import { execSync } from "child_process";
import fs from "fs";
function putText(n,s){const repo=process.env.GH_REPO,tok=process.env.GH_TOKEN;const url='https://api.github.com/repos/'+repo+'/contents/analize/'+n;let sha='';try{sha=JSON.parse(execSync('curl -s --max-time 30 -H "Authorization: Bearer '+tok+'" "'+url+'?ref=main&t='+Date.now()+'"',{encoding:'utf8'})).sha||'';}catch(e){}const b={message:'x',branch:'main',content:Buffer.from(s,'utf8').toString('base64')};if(sha)b.sha=sha;fs.writeFileSync('/tmp/pf.json',JSON.stringify(b));execSync('curl -s --max-time 40 -X PUT -H "Authorization: Bearer '+tok+'" -d @/tmp/pf.json "'+url+'"',{encoding:'utf8'});}
const BASE='https://dev.avesa.lt';const U=process.env.WP_USER||'';const P=(process.env.WP_APP_PASS||'').replace(/\s+/g,'');
const PHP="if ( ! defined('ABSPATH') ) { return; }\nadd_action('wp_loaded', function(){\n  if ( ! isset($_GET['ps_frontend_recon']) ) { return; }\n  if ( ($_GET['token']??'') !== 'cmplz_6680aa2a42151d54fa8d64ec' ) { return; }\n  $out = array();\n\n  // 1. Aktyvi tema\n  $theme = wp_get_theme();\n  $out['1_theme'] = array('name'=>$theme->get('Name'), 'parent'=>$theme->parent() ? $theme->parent()->get('Name') : null);\n\n  // 2. MyAccount page + endpoint'ai\n  $out['2_myaccount_id'] = get_option('woocommerce_myaccount_page_id');\n  $out['2_myaccount_url'] = get_permalink(get_option('woocommerce_myaccount_page_id'));\n  // WC account endpoints\n  $out['2_wc_endpoints'] = array(\n    'orders' => get_option('woocommerce_myaccount_orders_endpoint', 'orders'),\n    'edit-account' => get_option('woocommerce_myaccount_edit_account_endpoint', 'edit-account'),\n  );\n\n  // 3. Ar Flatsome UX builder / shortcode sistema\n  $out['3_flatsome_active'] = function_exists('flatsome_ux_builder') || defined('FLATSOME_VERSION');\n  if(defined('FLATSOME_VERSION')) $out['3_flatsome_version'] = FLATSOME_VERSION;\n\n  // 4. Ar galim registruoti custom endpoint (rewrite)\n  $out['4_permalink_structure'] = get_option('permalink_structure');\n\n  // 5. Petshop core klas\u0117s (patikra kad backend gyvas)\n  $out['5_backend'] = array(\n    'Pet_Profile' => class_exists('Petshop_Pet_Profile'),\n    'Pet_Products' => class_exists('Petshop_Pet_Products'),\n    'Magic_Login' => class_exists('Petshop_Magic_Login'),\n    'core_ver' => defined('PETSHOP_CORE_VERSION') ? PETSHOP_CORE_VERSION : '?',\n  );\n\n  // 6. Ar yra vieta kur galim d\u0117ti anket\u0105 (page/shortcode)\n  //    Patikrinam ar shortcode sistema veikia\n  $out['6_shortcode_support'] = function_exists('add_shortcode');\n\n  // 7. REST base URL (frontend JS naudos)\n  $out['7_rest_url'] = get_rest_url(null, 'petshop/v1');\n  $out['7_nonce_action'] = 'wp_rest';  // WP REST nonce\n\n  header('Content-Type: application/json; charset=utf-8');\n  echo wp_json_encode($out, JSON_UNESCAPED_UNICODE|JSON_UNESCAPED_SLASHES|JSON_PRETTY_PRINT);\n  exit;\n}, 6);";
function api(method,path,body){const auth='-u "'+U+':'+P+'"';let cmd;if(body){fs.writeFileSync('/tmp/b.json',JSON.stringify(body));cmd='curl -s -k -A "Mozilla/5.0" --max-time 90 -w "\nHTTP:%{http_code}" '+auth+' -X '+method+' -H "Content-Type: application/json" --data-binary @/tmp/b.json "'+BASE+path+'"';}else{cmd='curl -s -k -A "Mozilla/5.0" --max-time 60 -w "\nHTTP:%{http_code}" '+auth+' -X '+method+' "'+BASE+path+'"';}let r;try{r=execSync(cmd,{encoding:'utf8',maxBuffer:30000000});}catch(e){r=(e.stdout||'')+'\nHTTP:TIMEOUT';}return{code:(r.match(/HTTP:(\S+)$/)||[])[1]||'?',body:r.replace(/\nHTTP:\S+$/,'')};}
function sh(c){try{return execSync(c,{encoding:'utf8',maxBuffer:30000000});}catch(e){return (e.stdout||'')+'[ERR]';}}
(async()=>{let id=0;
  const c=api('POST','/wp-json/code-snippets/v1/snippets',{name:'Frontend Recon tmp',desc:'x',code:PHP,scope:'global',active:true,priority:10});
  try{id=JSON.parse(c.body).id;}catch(e){}
  if(!id){putText('_fr.txt','fail: '+c.body.slice(0,200));return;}
  execSync('sleep 2');
  const r=sh('curl -s -k -A "Mozilla/5.0" --max-time 45 "'+BASE+'/?ps_frontend_recon=1&token=cmplz_6680aa2a42151d54fa8d64ec"');
  putText('frontend_recon.json', r);
  api('POST','/wp-json/code-snippets/v1/snippets/'+id+'/deactivate',{});
})();
