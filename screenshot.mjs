import { execSync } from "child_process";
import fs from "fs";
function putText(n,s){const repo=process.env.GH_REPO,tok=process.env.GH_TOKEN;const url='https://api.github.com/repos/'+repo+'/contents/analize/'+n;let sha='';try{sha=JSON.parse(execSync('curl -s --max-time 30 -H "Authorization: Bearer '+tok+'" "'+url+'?ref=main&t='+Date.now()+'"',{encoding:'utf8'})).sha||'';}catch(e){}const b={message:'x',branch:'main',content:Buffer.from(s,'utf8').toString('base64')};if(sha)b.sha=sha;fs.writeFileSync('/tmp/pf.json',JSON.stringify(b));execSync('curl -s --max-time 40 -X PUT -H "Authorization: Bearer '+tok+'" -d @/tmp/pf.json "'+url+'"',{encoding:'utf8'});}
const BASE='https://dev.avesa.lt';const U=process.env.WP_USER||'';const P=(process.env.WP_APP_PASS||'').replace(/\s+/g,'');
function api(method,path,body){const auth='-u "'+U+':'+P+'"';let cmd;if(body){fs.writeFileSync('/tmp/b.json',JSON.stringify(body));cmd='curl -s -k --max-time 90 -w "\nHTTP:%{http_code}" '+auth+' -X '+method+' -H "Content-Type: application/json" --data-binary @/tmp/b.json "'+BASE+path+'"';}else{cmd='curl -s -k --max-time 60 -w "\nHTTP:%{http_code}" '+auth+' -X '+method+' "'+BASE+path+'"';}let r;try{r=execSync(cmd,{encoding:'utf8',maxBuffer:30000000});}catch(e){r=(e.stdout||'')+'\nHTTP:TIMEOUT';}return{code:(r.match(/HTTP:(\S+)$/)||[])[1]||'?',body:r.replace(/\nHTTP:\S+$/,'')};}
function sh(c){try{return execSync(c,{encoding:'utf8',maxBuffer:30000000});}catch(e){return (e.stdout||'')+'[ERR]';}}
const PHP="if(!defined('ABSPATH'))return;\nadd_action('wp_loaded', function(){\n  if(($_GET['ps_core_recon']??'')!=='1')return;\n  if(($_GET['token']??'')!=='cmplz_6680aa2a42151d54fa8d64ec')return;\n  $out=array();\n  $out['plugins_dir']=WP_PLUGIN_DIR;\n  $out['petshop_core_dir_exists']=is_dir(WP_PLUGIN_DIR.'/petshop-core');\n  $out['petshop_esp_dir_exists']=is_dir(WP_PLUGIN_DIR.'/petshop-esp');\n  $out['petshop_esp_files']=is_dir(WP_PLUGIN_DIR.'/petshop-esp/includes')?scandir(WP_PLUGIN_DIR.'/petshop-esp/includes'):null;\n  // Kokie WP options'e tokenai laikomi\n  $out['wp_options_esp']=array(\n    'sender_mk'=>strlen(get_option('petshop_esp_sender_mk',''))>0?'YRA (base64)':'nera',\n    'sender_tk'=>strlen(get_option('petshop_esp_sender_tk',''))>0?'YRA (base64)':'nera',\n    'webhook_secret'=>strlen(get_option('petshop_esp_sender_webhook_secret',''))>0?'YRA':'nera',\n  );\n  // Egzistuojanciu klasiu patvirtinimas\n  $out['classes']=array(\n    'Petshop_ESP_Event_Log'=>class_exists('Petshop_ESP_Event_Log'),\n    'Petshop_ESP_Consent_Log'=>class_exists('Petshop_ESP_Consent_Log'),\n    'Petshop_ESP_Consent_Sync'=>class_exists('Petshop_ESP_Consent_Sync'),\n    'Petshop_ESP_Retry_Queue'=>class_exists('Petshop_ESP_Retry_Queue'),\n    'Petshop_ESP_Webhook_Receiver'=>class_exists('Petshop_ESP_Webhook_Receiver'),\n    'Petshop_Sender_Adapter'=>class_exists('Petshop_Sender_Adapter'),\n    'Interface_ESP_Adapter'=>interface_exists('Interface_ESP_Adapter'),\n  );\n  // DB lenteles egzistuoja\n  global $wpdb;\n  $out['tables']=array(\n    'event_log'=>(bool)$wpdb->get_var(\"SHOW TABLES LIKE '{$wpdb->prefix}ps_event_log'\"),\n    'consent_log'=>(bool)$wpdb->get_var(\"SHOW TABLES LIKE '{$wpdb->prefix}ps_consent_log'\"),\n  );\n  header('Content-Type:application/json;charset=utf-8');\n  echo wp_json_encode($out,JSON_PRETTY_PRINT|JSON_UNESCAPED_SLASHES);\n  exit;\n}, 6);";
(async()=>{
  const c=api('POST','/wp-json/code-snippets/v1/snippets',{name:'Petshop Core Recon tmp',desc:'token',code:PHP,scope:'global',active:true,priority:10});
  let id=0;try{id=JSON.parse(c.body).id;}catch(e){}
  if(id){ execSync('sleep 2'); const r=sh('curl -s -k --max-time 30 "'+BASE+'/?ps_core_recon=1&token=cmplz_6680aa2a42151d54fa8d64ec"'); putText('_core_recon.txt', r); api('POST','/wp-json/code-snippets/v1/snippets/'+id+'/deactivate',{}); }
  else putText('_core_recon.txt', 'fail: '+c.body.slice(0,200));
})();
