{
 "v": "dep-164850",
 "kurimas": 200,
 "sid": 4751,
 "sa_push": {
  "status": 404,
  "body": "{\"code\":\"rest_no_route\",\"message\":\"Nerastas mar\\u0161rutas atitinkantis adres\\u0105 ir u\\u017eklausos metod\\u0105.\",\"data\":{\"status\":404}}"
 },
 "R": {
  "v": "S1614 e8r",
  "temp_istrinta": 1,
  "desk_lap_metodai": [
   "musu_puslapis",
   "eilutes_saltinis",
   "puslapis"
  ],
  "src_musu_puslapis": {
   "f": "petshop-desk.php:1051-1053",
   "src": [
    "\tprotected static function musu_puslapis() {",
    "\t\treturn isset( $_GET['page'] ) && self::SLUG === $_GET['page'];",
    "\t}"
   ]
  },
  "src_eilutes_saltinis": {
   "f": "petshop-desk.php:1088-1094",
   "src": [
    "\tprotected static function eilutes_saltinis( $item ) {",
    "\t\t$s = $item->get_meta( '_ps_source' );",
    "\t\tif ( is_string( $s ) && '' !== $s ) { return $s; }",
    "\t\t$v = self::sprendimas( $item->get_product_id(), $item->get_quantity() );",
    "\t\tif ( ! empty( $v['source'] ) && is_string( $v['source'] ) ) { return $v['source']; }",
    "\t\treturn '';",
    "\t}"
   ]
  },
  "src_puslapis": {
   "f": "petshop-desk.php:1576-1632",
   "src": [
    "\tpublic static function puslapis() {",
    "\t\tif ( ! current_user_can( 'edit_shop_orders' ) ) { wp_die( 'Nepakanka teisių' ); }",
    "",
    "\t\tif ( isset( $_GET['view'] ) && 'rytas' === $_GET['view'] ) { self::rytas(); return; }",
    "",
    "\t\t$eile = isset( $_GET['eile'] ) ? sanitize_key( $_GET['eile'] ) : 'nauji';",
    "\t\tif ( ! isset( self::EILES[ $eile ] ) ) { $eile = 'nauji'; }",
    "",
    "\t\t$f = array(",
    "\t\t\t'q'        => isset( $_GET['q'] ) ? sanitize_text_field( wp_unslash( $_GET['q'] ) ) : '',",
    "\t\t\t'data'     => isset( $_GET['data'] ) ? sanitize_key( $_GET['data'] ) : '',",
    "\t\t\t'nuo'      => isset( $_GET['nuo'] ) ? sanitize_text_field( wp_unslash( $_GET['nuo'] ) ) : '',",
    "\t\t\t'iki'      => isset( $_GET['iki'] ) ? sanitize_text_field( wp_unslash( $_GET['iki'] ) ) : '',",
    "\t\t\t'vykdymas' => isset( $_GET['vykdymas'] ) ? sanitize_key( $_GET['vykdymas'] ) : '',",
    "\t\t\t'vezejas'  => isset( $_GET['vezejas'] ) ? sanitize_key( $_GET['vezejas'] ) : '',",
    "\t\t\t'busena'   => isset( $_GET['busena'] ) ? sanitize_key( $_GET['busena'] ) : '',",
    "\t\t\t'mokejimas'=> isset( $_GET['mokejimas'] ) ? sanitize_key( $_GET['mokejimas'] ) : '',",
    "\t\t\t'amzius'   => isset( $_GET['amzius'] ) ? sanitize_key( $_GET['amzius'] ) : '',",
    "\t\t\t'nr'       => isset( $_GET['nr'] ) ? sanitize_text_field( wp_unslash( $_GET['nr'] ) ) : '',",
    "\t\t\t'klientas' => isset( $_GET['klientas'] ) ? sanitize_text_field( wp_unslash( $_GET['klientas'] ) ) : '',",
    "\t\t\t'tel'      => isset( $_GET['tel'] ) ? sanitize_text_field( wp_unslash( $_GET['tel'] ) ) : '',",
    "\t\t\t'adresas'  => isset( $_GET['adresas'] ) ? sanitize_text_field( wp_unslash( $_GET['adresas'] ) ) : '',",
    "\t\t\t'zvilgsnis'=> isset( $_GET['zvilgsnis'] ) ? sanitize_key( $_GET['zvilgsnis'] ) : '',",
    "\t\t);",
    "",
    "\t\t$eilutes = self::gauti( $eile, $f );",
    "\t\tif ( 'klausimai' === $eile || 'misrus' === $eile ) { $f['korteles'] = 1; }",
    "\t\t$c       = self::skaiciai();",
    "\t\t$statusai = wc_get_order_statuses();",
    "",
    "\t\tself::stilius();",
    "\t\techo '<div class=\"pd\">';",
    "\t\tself::virsus( $f );",
    "\t\tself::pranesimas();",
    "\t\techo '<div class=\"pd-body\">';",
    "\t\tself::rail( $eile, $c );",
    "\t\techo '<main class=\"pd-main\">';",
    "\t\tself::pipe( $c );",
    "\t\tself::juosta( $eile, $f, count( $eilutes ) );",
    "\t\tif ( 'klausimai' === $eile && $eilutes ) {",
    "\t\t\techo '<div class=\"pd-wrap\">';",
    "\t\t\tself::klausimu_korteles( $eilutes );",
    "\t\t\techo '</div>';",
    "\t\t} elseif ( 'misrus' === $eile && $eilutes ) {",
    "\t\t\techo '<div class=\"pd-wrap\">';",
    "\t\t\tself::misriu_korteles( $eilutes );",
    "\t\t\techo '</div>';",
    "\t\t} else {",
    "\t\t\tself::lentele( $eilutes, $statusai, $eile, $f );",
    "\t\t}",
    "\t\techo '</main></div>';",
    "\t\tself::skydelis();",
    "\t\tself::dialogas();",
    "\t\tself::bulk();",
    "\t\tself::skriptas();",
    "\t\techo '</div>';",
    "\t}"
   ]
  },
  "vp_dispatch_grep": [
   "304: $products_count = 0;",
   "305: foreach ( $order->get_items() as $product_item ) {",
   "306: $products_count += $product_item->get_quantity();",
   "311: 'products_count' => $products_count,",
   "472: $order_products = [];",
   "476: foreach ( $order->get_items() as $item_id => $product_item ) {",
   "489: $order_products[] = $product;",
   "634: foreach ($order_products as $product) {",
   "652: $total_weight = 0;",
   "661: $packs[$i]['weight'] = max($packs[$i]['weight'], 0.1);",
   "677: if ($is_global || $packs[$i]['weight'] > 0) {",
   "678: $weight = $document->createElement('weight', $packs[$i]['weight']);",
   "679: $pack->appendChild($weight);",
   "688: $total_weight += $packs[$i]['weight'];",
   "701: $weight = $document->createElement('weight', max($this->get_product_weight($product), 0.1));",
   "702: $pack->appendChild($weight);",
   "709: $total_weight += $this->get_product_weight($product);",
   "723: $pack_weight = 0;",
   "729: $pack_weight += $this->get_product_weight($products_without_shipments[$y]);",
   "735: $pack_weight = max(wc_get_weight($pack_weight, 'kg'), 0.1);",
   "736: $weight = $document->createElement('weight', $pack_weight);",
   "737: $pack->appendChild($weight);",
   "742: $total_weight += $pack_weight;",
   "749: $venipak_shipping_order_data['weight'] = $total_weight;",
   "750: $venipak_shipping_order_data['products_count'] = sizeof($order_products);",
   "759: public function get_product_weight($product) {",
   "761: $weight = $product->get_weight();",
   "762: if ($weight) {",
   "763: return $weight;"
  ],
  "desk_svoris": {
   "f": "petshop-desk.php:2535-2542",
   "src": [
    "\tprotected static function uzsakymo_svoris( $o ) {",
    "\t\t$kg = 0.0;",
    "\t\tforeach ( $o->get_items() as $it ) {",
    "\t\t\t$p = $it->get_product();",
    "\t\t\tif ( $p ) { $kg += (float) $p->get_weight() * (int) $it->get_quantity(); }",
    "\t\t}",
    "\t\treturn $kg;",
    "\t}"
   ]
  }
 }
}