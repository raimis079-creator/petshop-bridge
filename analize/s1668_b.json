{
 "v": "dep-164854",
 "kurimas": 200,
 "sid": 5372,
 "sa_push": {
  "status": 404,
  "body": "{\"code\":\"rest_no_route\",\"message\":\"Nerastas mar\\u0161rutas atitinkantis adres\\u0105 ir u\\u017eklausos metod\\u0105.\",\"data\":{\"status\":404}}"
 },
 "GO": {
  "v": "S1668 b",
  "ajax_av_irasyti": [
   "1015 \tpublic static function ajax_av_irasyti() {",
   "1016 \t\tif ( ! current_user_can( 'manage_woocommerce' ) ) { wp_send_json_error( 'nepakanka teisių', 403 ); }",
   "1017 \t\tcheck_ajax_referer( 'ps_kat', 'nonce' );",
   "1018 ",
   "1019 \t\t$priez = isset( $_POST['priezastis'] ) ? sanitize_key( $_POST['priezastis'] ) : '';",
   "1020 \t\tif ( ! isset( self::priezastys()[ $priez ] ) ) { wp_send_json_error( 'nenurodyta priežastis' ); }",
   "1021 ",
   "1022 \t\t$raw = isset( $_POST['pakeitimai'] ) ? wp_unslash( $_POST['pakeitimai'] ) : '';",
   "1023 \t\t$sar = json_decode( $raw, true );",
   "1024 \t\tif ( ! is_array( $sar ) || ! $sar ) { wp_send_json_error( 'nėra ką išsaugoti' ); }",
   "1025 \t\tif ( count( $sar ) > 500 ) { wp_send_json_error( 'per daug eilučių vienu kartu (riba 500)' ); }",
   "1026 \t\tif ( ! self::uztikrinti_zurnala() ) { wp_send_json_error( 'nepavyko paruošti žurnalo' ); }",
   "1027 ",
   "1028 \t\t/* 1) SKAIČIUOJAM — dar nieko nerašom */",
   "1029 \t\t$planas = array(); $klaidos = array();",
   "1030 \t\tforeach ( $sar as $e ) {",
   "1031 \t\t\t$pid = isset( $e['id'] ) ? (int) $e['id'] : 0;",
   "1032 \t\t\t$iv  = isset( $e['ivestis'] ) ? trim( (string) $e['ivestis'] ) : '';",
   "1033 \t\t\tif ( ! $pid || get_post_type( $pid ) !== 'product' ) { $klaidos[] = array( 'id' => $pid, 'kl' => 'nėra prekės' ); continue; }",
   "1034 \t\t\tif ( ! preg_match( '/^([+-]?)(\\d{1,6})$/', $iv, $m ) ) {",
   "1035 \t\t\t\t$klaidos[] = array( 'id' => $pid, 'kl' => 'netinkama reikšmė: ' . $iv ); continue;",
   "1036 \t\t\t}",
   "1037 \t\t\t$buvo = self::av_dabar( $pid );",
   "1038 \t\t\t$sk   = (int) $m[2];",
   "1039 \t\t\tif ( $m[1] === '+' )      { $tapo = (int) $buvo + $sk; }",
   "1040 \t\t\telseif ( $m[1] === '-' )  { $tapo = (int) $buvo - $sk; }",
   "1041 \t\t\telse                      { $tapo = $sk; }",
   "1042 ",
   "1043 \t\t\t$prasyta = $tapo;",
   "1044 \t\t\tif ( $tapo < 0 ) { $tapo = 0; }   /* neigiamo likučio nebūna */",
   "1045 \t\t\t$planas[] = array(",
   "1046 \t\t\t\t'pid' => $pid, 'laukas' => self::av_laukas( $pid ),",
   "1047 \t\t\t\t'buvo' => $buvo, 'tapo' => $tapo, 'prasyta' => $prasyta,",
   "1048 \t\t\t\t'pokytis' => $tapo - (int) $buvo,",
   "1049 \t\t\t);",
   "1050 \t\t}",
   "1051 \t\tif ( ! $planas ) { wp_send_json_error( array( 'zinute' => 'nėra galiojančių eilučių', 'klaidos' => $klaidos ) ); }",
   "1052 ",
   "1053 \t\t/* 2) RAŠOM */",
   "1054 \t\tglobal $wpdb;",
   "1055 \t\t$t   = self::zurnalo_lentele();",
   "1056 \t\t$op  = 'AV' . gmdate( 'ymdHis' ) . wp_rand( 10, 99 );",
   "1057 \t\t$now = current_time( 'mysql' );",
   "1058 \t\t$uid = get_current_user_id();",
   "1059 \t\t$ok  = array();",
   "1060 ",
   "1061 \t\t/* v7.7: GAVIMAS KURIA PARTIJA. Likutis, atsiradęs be partijos, neturi",
   "1062 \t\t   nei galiojimo, nei savikainos — o FEFO nurasymas tokio nemato. */",
   "1063 \t\t$gal_iki = isset( $_POST['geriausia_iki'] ) ? trim( (string) wp_unslash( $_POST['geriausia_iki'] ) ) : '';",
   "1064 \t\t$sav_nauja = isset( $_POST['savikaina'] ) ? trim( (string) wp_unslash( $_POST['savikaina'] ) ) : '';",
   "1065 ",
   "1066 \t\tforeach ( $planas as &$p ) {",
   "1067 \t\t\t$pokytis = (int) $p['pokytis'];",
   "1068 \t\t\tif ( $priez === 'gavimas' && $pokytis > 0 && class_exists( 'Petshop_Partijos' )",
   "1069 \t\t\t\t&& self::likutis_rankinis( $p['pid'] ) ) {",
   "1070 ",
   "1071 \t\t\t\t$sav = $sav_nauja;",
   "1072 \t\t\t\tif ( $sav === '' || (float) str_replace( ',', '.', $sav ) <= 0 ) {",
   "1073 \t\t\t\t\t$sv = Petshop_Partijos::svertine_savikaina( $p['pid'] );",
   "1074 \t\t\t\t\tif ( $sv === null || (float) $sv <= 0 ) { $sv = (float) get_post_meta( $p['pid'], '_cost_price', true ); }"
  ],
  "js_kat_av": [
   "300 *     Naudojamas TAS PATS `ps_kat_av` endpoint'as, kaip sarase — antro kelio",
   "865 add_action( 'wp_ajax_ps_kat_av', array( __CLASS__, 'ajax_av_irasyti' ) );",
   "3102 galejo, o pakeisti — ne. Cia naudojamas TAS PATS `ps_kat_av`",
   "7558 if(av.length) darbai.push(siusti(\"ps_kat_av\",av,priez));",
   "7924 fd.append(\"action\",\"ps_kat_av\"); fd.append(\"nonce\",NONCE);"
  ],
  "dl_atsargos": [
   "2632 \tpublic static function ajax_atsargos() {",
   "2633 \t\tif ( ! current_user_can( 'edit_shop_orders' ) || ! check_ajax_referer( 'ps_dl_zurnalas', 'n', false ) ) { wp_send_json_error( 'teisės', 403 ); }",
   "2634 \t\tglobal $wpdb; $src = sanitize_key( wp_unslash( $_GET['src'] ?? '' ) ); $z = absint( $_GET['zenklas'] ?? 0 ); $isp = ! empty( $_GET['isparduota'] ); $q = trim( (string) wp_unslash( $_GET['q",
   "2635 \t\tif ( ! $src || ( ! $z && ! $isp && mb_strlen( $q ) < 2 ) ) { wp_send_json_success( array() ); }",
   "2636 \t\t$sql = \"SELECT p.ID FROM {$wpdb->posts} p JOIN {$wpdb->postmeta} m ON m.post_id = p.ID AND m.meta_key = '_ps_sandelis' AND m.meta_value = %s LEFT JOIN {$wpdb->prefix}wc_product_meta_lookup",
   "2637 \t\tif ( $z ) { $sql .= \" JOIN {$wpdb->term_relationships} r ON r.object_id = p.ID JOIN {$wpdb->term_taxonomy} tt ON tt.term_taxonomy_id = r.term_taxonomy_id AND tt.taxonomy = 'product_brand' ",
   "2638 \t\t$sql .= \" WHERE p.post_status = 'publish' AND p.post_type = 'product'\"; if ( $isp ) { $sql .= \" AND ( l.stock_quantity IS NULL OR l.stock_quantity <= 0 OR l.stock_status = 'outofstock' )\";",
   "2639 \t\tif ( mb_strlen( $q ) >= 2 ) { $like = '%' . $wpdb->esc_like( $q ) . '%'; $sql .= ' AND ( p.post_title LIKE %s OR l.sku LIKE %s )'; $w[] = $like; $w[] = $like; }",
   "2640 \t\t$sql .= ' ORDER BY p.post_title ASC LIMIT 300';",
   "2641 \t\t$ids = $wpdb->get_col( $wpdb->prepare( $sql, $w ) ); if ( ! $ids ) { wp_send_json_success( array() ); }",
   "2642 \t\t$pask = array(); if ( class_exists( 'Petshop_Partijos' ) && $wpdb->get_var( \"SHOW TABLES LIKE '{$wpdb->prefix}ps_partijos'\" ) ) { foreach ( (array) $wpdb->get_results( \"SELECT product_id, ",
   "2643 \t\t$tk = 'vf' === $src ? '_vf_qty' : ''; $ck = array( 'vf' => '_vf_cost', 'zb' => '_zb_cost' ); $out = array();",
   "2644 \t\tforeach ( $ids as $id ) { $p = wc_get_product( (int) $id ); if ( ! $p ) { continue; } $pr = $pask[ (int) $id ] ?? null; $sav = $pr ? (float) $pr->savikaina_eur : ( isset( $ck[ $src ] ) ? (",
   "2645 \t\t\t$out[] = array( 'id' => (int) $id, 'n' => $p->get_name(), 'sku' => (string) $p->get_sku(), 'av' => class_exists( 'Petshop_AV_Stock' ) ? (int) Petshop_AV_Stock::qty( (int) $id ) : (int) $p",
   "2646 \t\twp_send_json_success( $out );",
   "2647 \t}",
   "2648 ",
   "2649 \t/** v3.39: AJAX `ps_dl_vidine` — darbuotojo vidinė pastaba prie užsakymo (POST id, n, tekstas ≤ 2000). Klientui nerodoma. */",
   "2650 \tpublic static function ajax_vidine() {",
   "2651 \t\tif ( ! current_user_can( 'edit_shop_orders' ) ) { wp_send_json_error( 'teisės', 403 ); }",
   "2652 \t\t$id = absint( $_POST['id'] ?? 0 ); if ( ! $id || ! check_ajax_referer( 'ps_dl_vidine_' . $id, 'n', false ) ) { wp_send_json_error( 'nonce', 403 ); }",
   "2653 \t\t$o = wc_get_order( $id ); if ( ! $o ) { wp_send_json_error( 'nėra užsakymo' ); }",
   "2654 \t\t$t = mb_substr( sanitize_textarea_field( wp_unslash( $_POST['tekstas'] ?? '' ) ), 0, 2000 ); $u = wp_get_current_user(); $buvo = (string) $o->get_meta( '_ps_vidine_pastaba' );",
   "2655 \t\t$kas = '' === $t ? '' : current_time( 'mysql' ) . '|' . $u->display_name;",
   "2656 \t\tif ( '' === $t ) { $o->delete_meta_data( '_ps_vidine_pastaba' ); $o->delete_meta_data( '_ps_vidine_pastaba_kas' ); } else { $o->update_meta_data( '_ps_vidine_pastaba', $t ); $o->update_met",
   "2657 \t\t$o->save();",
   "2658 \t\tif ( class_exists( 'Petshop_Uzsakymu_Ivykiai' ) && $buvo !== $t ) { Petshop_Uzsakymu_Ivykiai::irasyti( array( 'uzsakymas' => $id, 'sritis' => 'desk', 'veiksmas' => 'vidine_pastaba', 'rezul",
   "2659 \t\twp_send_json_success( array( 'kas' => $kas, 'tekstas' => $t ) );",
   "2660 \t}",
   "2661 ",
   "2662 \tpublic static function ajax_prekes() {",
   "2663 \t\tif ( ! current_user_can( 'edit_shop_orders' ) || ! check_ajax_referer( 'ps_dl_zurnalas', 'n', false ) ) { wp_send_json_error( 'teisės', 403 ); }",
   "2664 \t\tglobal $wpdb; $q = trim( (string) wp_unslash( $_GET['q'] ?? '' ) ); if ( mb_strlen( $q ) < 2 ) { wp_send_json_success( array() ); }",
   "2665 \t\t$like = '%' . $wpdb->esc_like( $q ) . '%'; $out = array();",
   "2666 \t\t$ids = $wpdb->get_col( $wpdb->prepare( \"SELECT p.ID FROM {$wpdb->posts} p JOIN {$wpdb->prefix}wc_product_meta_lookup l ON l.product_id = p.ID WHERE p.post_status = 'publish' AND p.post_typ",
   "2667 \t\tforeach ( (array) $ids as $id ) { $p = wc_get_product( (int) $id ); if ( ! $p || $p->is_type( 'variable' ) || '' === $p->get_price() ) { continue; }",
   "2668 \t\t\t$out[] = array( 'id' => (int) $id, 'n' => $p->get_name(), 'sku' => (string) $p->get_sku(), 'kaina' => round( (float) wc_get_price_including_tax( $p ), 2 ), 'reg' => round( (float) wc_get_",
   "2669 \t\twp_send_json_success( $out );",
   "2670 \t}",
   "2671 ",
   "2672 \t/** POST `ps_dl_naujas` — telefoninis užsakymas (Raimis 09-05 B): svečio (arba rasto vartotojo) užsakymas `created_via=darbalaukis`, prekės parduotuvės kainomis (darbuotojo įrašytomis, su P",
   "2673 \t *  nuolaida (€, proporcingai eilutėms — WC „nuolaida“, pastaba privaloma), pristatymas iš WC zonos instancijų (kaina darbuotojo), Venipak paštomatas per plugino `venipak_store_order_pickup",
   "2674 \t *  LP terminalas plugino meta. Apmokėjimas: `pavedimu` → bacs + on-hold (WC / temos srautas kaip kasoje: laiškas su išankstine; v3.34: WC likutis nurašomas tik apmokėjus — `telefonu_likuti",
   "2675 \tpublic static function naujas_vykdyti() {",
   "2676 \t\tif ( ! current_user_can( 'edit_shop_orders' ) ) { wp_die( 'Nepakanka teisių' ); }"
  ],
  "dl_eil": [
   "16 *   sujungiama su esama eilute); AJAX `ps_dl_atsargos` (src, zenklas, isparduota, q; ≤300 eil.); (3) atsargų eilutės kortelėje — kiekis taisomas (onchange) ir „×“ (`ka=eilute`; tik `order_id",
   "142 *   (laikas|kas|nr) — `faktai()` jas išima iš dalių (av_side/tiesiai), žingsnelių nėra, užraktas „atšaukta“; likutis: tiekėjo eilutės +q į `_own_stock_qty`",
   "211 *   „Atšaukti — prekės grįžo į AV“ (`grizta_atsaukti`): dropship eilutėms +q į `_own_stock_qty` (`Petshop_AV_Stock::increase` — sukuria lauką, prekė tampa",
   "367 add_action( 'wp_ajax_ps_dl_atsargos', array( __CLASS__, 'ajax_atsargos' ) ); // v3.38: prekių rinkiklis „į atsargas“",
   "1274 /** Eilutės AV likučio judesys: +q / −q pagal prekės rūšį (grynai AV → `_stock`, AV+tiekėjas → `_own_stock_qty`). */",
   "2631 /** v3.38: AJAX `ps_dl_atsargos` — tiekėjo prekės rinkikliui: src (`_ps_sandelis`), zenklas (product_brand term_id), isparduota (WC likutis ≤ 0 = katalogo „reikia užsakyti“), q (filtras). ≤ ",
   "3108 *  → cancelled (AV grąžina variklis, dropship +q). Kai yra — tik grįžusi dalis: eilutės `_ps_atsaukta`, likutis (dropship +q `_own_stock_qty`; AV — variklio",
   "4180 fetch(ajaxurl+'?action=ps_dl_atsargos&n='+encodeURIComponent(r.getAttribute('data-n'))+'&src='+encodeURIComponent(r.getAttribute('data-src'))+'&zenklas='+encodeURIComponent(z)+'&isparduota='"
  ]
 }
}