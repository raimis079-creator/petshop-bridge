{
 "v": "dep-174853",
 "kurimas": 200,
 "sid": 5386,
 "sa_push": {
  "status": 404,
  "body": "{\"code\":\"rest_no_route\",\"message\":\"Nerastas mar\\u0161rutas atitinkantis adres\\u0105 ir u\\u017eklausos metod\\u0105.\",\"data\":{\"status\":404}}"
 },
 "GO": {
  "v": "S1670 e",
  "rasymas": [
   "1053 \t\t\t$pid = isset( $e['id'] ) ? (int) $e['id'] : 0;",
   "1054 \t\t\t$iv  = isset( $e['ivestis'] ) ? trim( (string) $e['ivestis'] ) : '';",
   "1055 \t\t\tif ( ! $pid || get_post_type( $pid ) !== 'product' ) { $klaidos[] = array( 'id' => $pid, 'kl' => 'nėra prekės' ); continue; }",
   "1056 \t\t\tif ( ! preg_match( '/^([+-]?)(\\d{1,6})$/', $iv, $m ) ) {",
   "1057 \t\t\t\t$klaidos[] = array( 'id' => $pid, 'kl' => 'netinkama reikšmė: ' . $iv ); continue;",
   "1058 \t\t\t}",
   "1059 \t\t\t$buvo = self::av_dabar( $pid );",
   "1060 \t\t\t$sk   = (int) $m[2];",
   "1061 \t\t\tif ( $m[1] === '+' )      { $tapo = (int) $buvo + $sk; }",
   "1062 \t\t\telseif ( $m[1] === '-' )  { $tapo = (int) $buvo - $sk; }",
   "1063 \t\t\telse                      { $tapo = $sk; }",
   "1064 ",
   "1065 \t\t\t$prasyta = $tapo;",
   "1066 \t\t\tif ( $tapo < 0 ) { $tapo = 0; }   /* neigiamo likučio nebūna */",
   "1067 \t\t\t$planas[] = array(",
   "1068 \t\t\t\t'pid' => $pid, 'laukas' => self::av_laukas( $pid ),",
   "1069 \t\t\t\t'buvo' => $buvo, 'tapo' => $tapo, 'prasyta' => $prasyta,",
   "1070 \t\t\t\t'pokytis' => $tapo - (int) $buvo,",
   "1071 \t\t\t);",
   "1072 \t\t}",
   "1073 \t\tif ( ! $planas ) { wp_send_json_error( array( 'zinute' => 'nėra galiojančių eilučių', 'klaidos' => $klaidos ) ); }",
   "1074 ",
   "1075 \t\t/* 2) RAŠOM */",
   "1076 \t\tglobal $wpdb;",
   "1077 \t\t$t   = self::zurnalo_lentele();",
   "1078 \t\t$op  = 'AV' . gmdate( 'ymdHis' ) . wp_rand( 10, 99 );",
   "1079 \t\t$now = current_time( 'mysql' );",
   "1080 \t\t$uid = get_current_user_id();",
   "1081 \t\t$ok  = array();",
   "1082 ",
   "1083 \t\t/* v7.7: GAVIMAS KURIA PARTIJA. Likutis, atsiradęs be partijos, neturi",
   "1084 \t\t   nei galiojimo, nei savikainos — o FEFO nurasymas tokio nemato. */",
   "1085 \t\t$gal_iki = isset( $_POST['geriausia_iki'] ) ? trim( (string) wp_unslash( $_POST['geriausia_iki'] ) ) : '';",
   "1086 \t\t$sav_nauja = isset( $_POST['savikaina'] ) ? trim( (string) wp_unslash( $_POST['savikaina'] ) ) : '';",
   "1087 ",
   "1088 \t\tforeach ( $planas as &$p ) {",
   "1089 \t\t\t$pokytis = (int) $p['pokytis'];",
   "1090 \t\t\tif ( $priez === 'gavimas' && $pokytis > 0 && class_exists( 'Petshop_Partijos' )",
   "1091 \t\t\t\t&& self::likutis_rankinis( $p['pid'] ) ) {",
   "1092 ",
   "1093 \t\t\t\t$sav = $sav_nauja;",
   "1094 \t\t\t\tif ( $sav === '' || (float) str_replace( ',', '.', $sav ) <= 0 ) {",
   "1095 \t\t\t\t\t$sv = Petshop_Partijos::svertine_savikaina( $p['pid'] );",
   "1096 \t\t\t\t\tif ( $sv === null || (float) $sv <= 0 ) { $sv = (float) get_post_meta( $p['pid'], '_cost_price', true ); }",
   "1097 \t\t\t\t\t$sav = ( (float) $sv > 0 ) ? (string) $sv : '';",
   "1098 \t\t\t\t}",
   "1099 \t\t\t\tif ( $sav !== '' ) {",
   "1100 \t\t\t\t\t$r = Petshop_Partijos::priimti( $p['pid'], array(",
   "1101 \t\t\t\t\t\t'kiekis'        => $pokytis,",
   "1102 \t\t\t\t\t\t'savikaina'     => $sav,",
   "1103 \t\t\t\t\t\t'valiuta'       => 'EUR',",
   "1104 \t\t\t\t\t\t'kursas'        => 1,",
   "1105 \t\t\t\t\t\t'geriausia_iki' => $gal_iki,",
   "1106 \t\t\t\t\t\t'tiekejas'      => (string) get_post_meta( $p['pid'], '_ps_sandelis', true ),",
   "1107 \t\t\t\t\t\t'gauta'         => current_time( 'Y-m-d' ),",
   "1108 \t\t\t\t\t\t'pastaba'       => 'Likučio įvedimas kortelėje',",
   "1109 \t\t\t\t\t) );",
   "1110 \t\t\t\t\tif ( ! is_wp_error( $r ) ) {",
   "1111 \t\t\t\t\t\t/* `priimti()` jau pakele likuti — tiesiogiai nebesirasom. */",
   "1112 \t\t\t\t\t\t$p['partija'] = isset( $r['partijos_id'] ) ? (int) $r['partijos_id'] : 0;",
   "1113 \t\t\t\t\t\t$p['tapo']    = isset( $r['likutis'] ) ? (int) $r['likutis'] : $p['tapo'];",
   "1114 \t\t\t\t\t\t$p['per_partija'] = true;",
   "1115 \t\t\t\t\t}",
   "1116 \t\t\t\t}",
   "1117 \t\t\t}",
   "1118 ",
   "1119 \t\t\t/* v7.8: MAZINIMAS — nurasom is partiju (FEFO), o ne tik is likucio.",
   "1120 \t\t\t   Kitaip partiju suma ir likutis issiskiria, ir savikaina bei",
   "1121 \t\t\t   galiojimai pradeda meluoti. */",
   "1122 \t\t\tif ( $pokytis < 0 && class_exists( 'Petshop_Partijos' ) && self::likutis_rankinis( $p['pid'] ) ) {",
   "1123 \t\t\t\t$turi = Petshop_Partijos::partijos( $p['pid'], true );",
   "1124 \t\t\t\tif ( $turi ) {",
   "1125 \t\t\t\t\t$r = Petshop_Partijos::nurasyti( $p['pid'], abs( $pokytis ) );",
   "1126 \t\t\t\t\tif ( ! is_wp_error( $r ) ) {",
   "1127 \t\t\t\t\t\t$p['per_partija'] = true;",
   "1128 \t\t\t\t\t\tif ( isset( $r['likutis'] ) ) { $p['tapo'] = (int) $r['likutis']; }",
   "1129 \t\t\t\t\t}",
   "1130 \t\t\t\t}",
   "1131 \t\t\t}",
   "1132 \t\t}",
   "1133 \t\tunset( $p );",
   "1134 ",
   "1135 \t\tforeach ( $planas as $p ) {"
  ],
  "petshop-wc-sync.php": [
   "11 * kainą ir kategorijų sąrašai. Rašant tiesiai per update_post_meta ta lentelė",
   "56 '_stock', '_stock_status', '_manage_stock',",
   "67 public static function init() {",
   "69 add_action( $h, array( __CLASS__, 'meta_pakeista' ), 999, 3 );",
   "71 add_action( 'deleted_post_meta', array( __CLASS__, 'meta_pakeista' ), 999, 3 );",
   "74 add_action( 'woocommerce_before_product_object_save', array( __CLASS__, 'wc_pradeda' ), 1 );",
   "75 add_action( 'woocommerce_after_product_object_save', array( __CLASS__, 'wc_baigia' ), 999 );",
   "77 add_action( 'shutdown', array( __CLASS__, 'atnaujinti' ), 1 );",
   "79 add_action( 'wp_loaded', array( __CLASS__, 'auditas' ), 5 );",
   "82 public static function wc_pradeda() { self::$wc_dirba = true; }",
   "83 public static function wc_baigia()  { self::$wc_dirba = false; }",
   "90 public static function meta_pakeista( $mid, $pid, $key ) {",
   "102 public static function atnaujinti() {",
   "134 private static function lookup_metodas() {",
   "156 public static function nesutapimai() {",
   "170 INNER JOIN {$p}postmeta m ON m.post_id = l.product_id AND m.meta_key = '_stock'",
   "175 INNER JOIN {$p}postmeta m ON m.post_id = l.product_id AND m.meta_key = '_stock_status'",
   "189 public static function suderinti( $limit = 500 ) {",
   "197 LEFT JOIN {$p}postmeta ms ON ms.post_id = l.product_id AND ms.meta_key = '_stock'",
   "198 LEFT JOIN {$p}postmeta mt ON mt.post_id = l.product_id AND mt.meta_key = '_stock_status'",
   "222 public static function auditas() {"
  ],
  "petshop-seseliai.php": [
   "15 *   - Petshop_Fulfillment::recalculate() — WC `_stock` = tiekėjo kiekis",
   "31 public static function init() {",
   "32 add_action( 'pmxi_after_xml_import', [ __CLASS__, 'po_importo' ], 20, 1 );",
   "33 add_action( 'ps_seseliai_valandinis', [ __CLASS__, 'sync' ] );",
   "40 public static function po_importo( $import_id ) {",
   "46 public static function sync( $kas = 'cron' ) {",
   "78 update_post_meta( $a, '_zb_qty', $qty );",
   "79 if ( $cost !== '' ) { update_post_meta( $a, '_zb_cost', $cost ); }",
   "80 if ( $sync !== '' ) { update_post_meta( $a, '_zb_last_sync', $sync ); }"
  ],
  "petshop-av-limit.php": [
   "7 * PROBLEMA: WooCommerce riboja pagal `_stock` — VIENO šaltinio kiekį.",
   "17 *   - GRYNAI AV prekėms nieko nekeičia — jų `_stock` ir taip teisingas",
   "20 *   - NEKEIČIA `_stock` reikšmės (sync ją perrašo)",
   "23 * SAUGIKLIS: veikia TIK toms prekėms, kurios turi `_own_stock_qty` IR tiekėjo",
   "30 public static function init() {",
   "32 add_filter( 'woocommerce_product_get_stock_quantity', [ __CLASS__, 'stock_qty' ], 20, 2 );",
   "33 add_filter( 'woocommerce_product_variation_get_stock_quantity', [ __CLASS__, 'stock_qty' ], 20, 2 );",
   "35 add_filter( 'woocommerce_product_get_stock_status', [ __CLASS__, 'stock_status' ], 20, 2 );",
   "37 add_filter( 'woocommerce_get_availability_text', [ __CLASS__, 'availability_text' ], 20, 2 );",
   "38 add_filter( 'woocommerce_get_stock_html', [ __CLASS__, 'stock_html' ], 20, 2 );",
   "42 protected static function taikoma( $product_id ) {",
   "52 public static function stock_qty( $qty, $product ) {",
   "57 $tiek = (int) $qty;                                  // WC `_stock` = tiekėjo kiekis",
   "62 public static function stock_status( $status, $product ) {",
   "72 public static function availability_text( $text, $product ) {",
   "75 if ( ! $product->is_in_stock() ) { return $text; }",
   "81 public static function stock_html( $html, $product ) {",
   "83 if ( ! is_a( $product, 'WC_Product' ) || ! $product->is_in_stock() ) { return $html; }"
  ]
 }
}