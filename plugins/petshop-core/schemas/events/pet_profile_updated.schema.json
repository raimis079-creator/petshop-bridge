{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "pet_profile_updated",
  "description": "Augintinio anketa atnaujinta (M7). Payload iskart nurodo pakeistus laukus.",
  "schema_version": 1,
  "type": "object",
  "required": ["schema_version", "event_name", "event_id", "emitted_at", "email", "payload"],
  "properties": {
    "schema_version": {"const": 1},
    "event_name": {"const": "pet_profile_updated"},
    "event_id": {"type": "string"},
    "emitted_at": {"type": "string", "format": "date-time"},
    "email": {"type": "string", "format": "email"},
    "customer_id": {"type": "integer"},
    "payload": {
      "type": "object",
      "required": ["pet_id", "changed_fields"],
      "properties": {
        "pet_id": {"type": "integer"},
        "changed_fields": {
          "type": "object",
          "additionalProperties": {
            "type": "object",
            "required": ["from", "to"],
            "properties": {
              "from": {},
              "to": {}
            }
          }
        }
      }
    }
  }
}
