{"result":{"s1496":": kitos gamintoju asys — amzius / kuno bukle / laikymas. Grazina array(asis, kodas, raw)\n  $ax2 = function($h){\n    $raw = trim(preg_replace('/\\s+/u',' ',strip_tags($h)));\n    $l = mb_strtolower($raw);\n    if(preg_match('/mėn|men\\.|mėnes|month|savait|week/u',$l)){\n      $code = preg_replace('/[^0-9\\-]/u','', str_replace(array('–','—'),'-',$l));\n      if($code==='') return null;\n      return array('age_range','m'.$code,$raw);\n    }\n    if(preg_match('/lies|antsvor|nutuk|normali kūno|normal weight|lean|overweight/u',$l)){\n      $c = preg_match('/lies|lean/u',$l)?'lean':(preg_match('/antsvor|nutuk|overweight/u',$l)?'overweight':'normal');\n      return array('body_condition',$c,$raw);\n    }\n    if(preg_match('/^bute|patalp|indoor|lauke|outdoor|kieme/u',$l)){\n      return array('housing', preg_match('/lauk|outdoor|kiem/u',$l)?'outdoor':'indoor', $raw);\n    }\n    return null;\n  };\n\n  // ---- prekiu sarasas: cat72/81 publish, be aktyvios lenteles\n  $prods=$wpdb->get_results(\"SELECT DISTINCT p.ID,p.post_title,p.post_content FROM {$wpdb->posts} p\n    JOIN {$wpdb->term_relationships} tr ON tr.object_id=p.ID\n    JOIN {$wpdb->term_taxonomy} tt ON tt.term_taxonomy_id=tr.term_taxonomy_id AND tt.taxonomy='product_cat' AND tt.term_id IN (72,81)\n    WHERE p.post_type='product' AND p.post_status='publish'\", ARRAY_A);\n  $mapped=array();\n  foreach((array)$wpdb->get_col(\"SELECT DISTINCT m.product_id FROM {$pf}ps_feeding_map m JOIN {$pf}ps_feeding_tables t ON t.id=m.feeding_table_id WHERE m.is_active=1 AND t.is_active=1\") as $i) $mapped[(int)$i]=true;\n\n  $out=array('apply'=>$apply,'ok'=>array(),'ambiguous'=>array(),'tables'=>0,'maps'=>0);\n  $reuse=array();\n\n  foreach($prods as $P){\n    $pid=(int)$P['ID'];\n    if(isset($mapped[$pid])) continue;\n    $title=$P['post_title'];\n\n    $brand=''; $bt=wp_get_post_terms($pid,'product_brand',array('fields'=>'names'));\n    if(!is_wp_error($bt)&&$bt) $brand=$bt[0];\n    if(!$brand) $brand=(string)get_post_meta($pid,'_legacy_manufacturer',true);\n    if(!$brand) $brand='(be brendo)';\n    if($only && mb_strtolower($brand)!==mb_strtolower($only)) continue;\n\n    $c=$P['post_content'];\n    if(!preg_match('/\\x{0160}\\x{0117}rimo|\\x{0161}\\x{0117}rimo|Paros norm|Maitinimo lentel/iu',$c)) continue;\n    if(!preg_match('/<table/i',$c)) continue;\n\n    // --- DOM: visos lenteles, renkam tinkamiausia\n    $html='<?xml encoding=\"UTF-8\"><div>'.$c.'</div>';\n    libxml_use_internal_errors(true);\n    $doc=new DOMDocument(); $doc->loadHTML($html); libxml_clear_errors();\n    $tabs=$doc->getElementsByTagName('table');\n    $grid=null;\n    foreach($tabs as $","s1490":"add_action('wp_loaded', function(){\n  if(!isset($_GET['ps_fixh']) || $_GET['ps_fixh']!=='Fh24x') return;\n  global $wpdb; $pf=$wpdb->prefix; $now=current_time('mysql'); $o=array();\n  $ids=$wpdb->get_col(\"SELECT id FROM {$pf}ps_feeding_tables WHERE verified_by='auto_parser_s228'\");\n  $o['lenteliu']=count($ids); $n=0;\n  foreach($ids as $tid){\n    $rows=$wpdb->get_results($wpdb->prepare(\"SELECT weight_from_kg,weight_to_kg,amount_from_g,amount_to_g,condition_dimensions,cell_type FROM {$pf}ps_feeding_rows WHERE feeding_table_id=%d ORDER BY row_order\",$tid),ARRAY_A);\n    $can=array();\n    foreach($rows as $r){ $can[]=implode('|',array(rtrim(rtrim(number_format((float)$r['weight_from_kg'],2,'.',''),'0'),'.'),rtrim(rtrim(number_format((float)$r['weight_to_kg'],2,'.',''),'0'),'.'),rtrim(rtrim(number_format((float)$r['amount_from_g'],2,'.',''),'0'),'.'),rtrim(rtrim(number_format((float)$r['amount_to_g'],2,'.',''),'0'),'.'),(string)$r['condition_dimensions'],(string)$r['cell_type'])); }\n    sort($can);\n    $h=hash('sha256',implode(\"\\n\",$can));\n    $wpdb->update($pf.'ps_feeding_tables',array('canonical_table_hash'=>$h,'canonical_hash_version'=>'chash_v1','weight_basis_source'=>'stated_in_source','redirect_row_count'=>0,'updated_at'=>$now),array('id'=>$tid));\n    $n++;\n  }\n  $o['atnaujinta']=$n;\n  $tests=array(array(16854,20),array(21014,15),array(14791,10),array(14281,20),array(16678,25),array(17283,12),array(18379,10));\n  foreach($tests as $t){\n    $r=Petshop_Feeding_Service::calc(array('product_id'=>$t[0],'weight_kg'=>$t[1],'species_code'=>'dog'));\n    $o['calc'][$t[0]]=array('t'=>mb_substr(html_entity_decode(get_the_title($t[0])),0,30),'w'=>$t[1],'st'=>$r['status'],'g'=>($r['norm_min_g']??null).'-'.($r['norm_max_g']??null),'d'=>$r['days_min']??null,'eur'=>$r['cost_day_min']??null,'am'=>$r['activity_mode']??null,'rc'=>implode(',',(array)($r['reason_codes']??array())));\n  }\n  header('Content-Type: application/json'); echo json_encode($o); exit;\n});\n","s1498":": kitos gamintoju asys — amzius / kuno bukle / laikymas. Grazina array(asis, kodas, raw)\n  $ax2 = function($h){\n    $raw = trim(preg_replace('/\\s+/u',' ',strip_tags($h)));\n    $l = mb_strtolower($raw);\n    if(preg_match('/mėn|men\\.|mėnes|month|savait|week/u',$l)){\n      $code = preg_replace('/[^0-9\\-]/u','', str_replace(array('–','—'),'-',$l));\n      if($code==='') return null;\n      return array('age_range','m'.$code,$raw);\n    }\n    if(preg_match('/^lies|antsvor|nutuk|^normali$|^normal$|lean|overweight/u',$l)){\n      $c = preg_match('/lies|lean/u',$l)?'lean':(preg_match('/antsvor|nutuk|overweight/u',$l)?'overweight':'normal');\n      return array('body_condition',$c,$raw);\n    }\n    if(preg_match('/bute|patalp|indoor|namuose|namie|lauke|outdoor|kieme|^aktyvus$/u',$l)){\n      $in_=preg_match('/bute|patalp|indoor|namuose|namie/u',$l); $out_=preg_match('/lauk|outdoor|kiem/u',$l);\n      if(preg_match('/^aktyvus$/u',$l)) $c='active';\n      elseif($in_&&$out_) $c='mixed';\n      elseif($out_) $c='outdoor';\n      else $c='indoor';\n      return array('housing',$c,$raw);\n    }\n    return null;\n  };\n\n  // ---- prekiu sarasas: cat72/81 publish, be aktyvios lenteles\n  $prods=$wpdb->get_results(\"SELECT DISTINCT p.ID,p.post_title,p.post_content FROM {$wpdb->posts} p\n    JOIN {$wpdb->term_relationships} tr ON tr.object_id=p.ID\n    JOIN {$wpdb->term_taxonomy} tt ON tt.term_taxonomy_id=tr.term_taxonomy_id AND tt.taxonomy='product_cat' AND tt.term_id IN (72,81)\n    WHERE p.post_type='product' AND p.post_status='publish'\", ARRAY_A);\n  $mapped=array();\n  foreach((array)$wpdb->get_col(\"SELECT DISTINCT m.product_id FROM {$pf}ps_feeding_map m JOIN {$pf}ps_feeding_tables t ON t.id=m.feeding_table_id WHERE m.is_active=1 AND t.is_active=1\") as $i) $mapped[(int)$i]=true;\n\n  $out=array('apply'=>$apply,'ok'=>array(),'ambiguous'=>array(),'tables'=>0,'maps'=>0);\n  $reuse=array();\n\n  foreach($prods as $P){\n    $pid=(int)$P['ID'];\n    if(isset($mapped[$pid])) continue;\n    $title=$P['post_title'];\n\n    $brand=''; $bt=wp_get_post_terms($pid,'product_brand',array('fields'=>'names'));\n    if(!is_wp_error($bt)&&$bt) $brand=$bt[0];\n    if(!$brand) $brand=(string)get_post_meta($pid,'_legacy_manufacturer',true);\n    if(!$brand) $brand='(be brendo)';\n    if($only && mb_strtolower($brand)!==mb_strtolower($only)) continue;\n\n    $c=$P['post_content'];\n    if(!preg_match('/\\x{0160}\\x{0117}rimo|\\x{0161}\\x{0117}rimo|Paros norm|Maitinimo lentel/iu',$c)) continue;\n    if(!preg_match('/<table/i',$c)) continue;\n\n    // --- DOM: visos lenteles, renkam tinkamiausia\n    $html='<?xml encoding=\"UTF-8\""}}