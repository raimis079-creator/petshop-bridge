{"versija":"P1B-1","temp_isjungta":[],"snip_id":3290,"rez":{"v":"P1B-MAGIC","magic":{"B":27630,"routes":["petshop/v1/magic-login/request"],"fn":["init","register_routes","claim_draft","resolve_draft_for_email","handle_request","handle_login_page","render_confirmation","process_login","send_login_email","unique_login_from_email","check_rate_limit","get_client_ip","is_login_path","render_retry","go","render_error","render_page"],"metodai":["POST","GET"],"ctx":{"peek":"e-use (M6 consume -> status used). * 5. Scanner-safe: GET = confirmation page (peek, be login), POST = login (consume). * Email antivirusai atidaro GET — negalima auto-login ant GET. * 6. Session ID regeneracija po login (session fi","consume":" per langa). * 3. TTL 15 min (trumpesnis nei default 30). * 4. Single-use (M6 consume -> status used). * 5. Scanner-safe: GET = confirmation page (peek, be login), POST = login (consume). * Email antivirusai atidaro GET — negalima ","token":"): VIENINGAS LOGIN. (1) handle_request priima 'context' (pet|account), * saugo token'o action lauke — redirect po prisijungimo pagal tai, is kur zmogus atejo: * augintinio juodrastis -> /augintinis/ (perkelimas), iprastas login -> pasky","expires":"eilgiau nei pats draftas — pasenusi busena negali nulemti nauju claim'u. 'expires_at' => $dabar + 14 * DAY_IN_SECONDS, ) ); return 'duplicate_candidate'; } // ★ Nuo cia — VISOS likusios sakos yra NESEKMINGOS arba TERM"}},"drafts":{"begin_claim":"function begin_claim( $draft_id, $user_id, $email ) { global $wpdb; $draft_id = (string) $draft_id; $row = self::get( $draft_id ); if ( ! $row ) { return array( 'ok' => false, 'reason' => 'not_found', 'row' => null, 'attempt_id' => '' ); } // Jau perkeltas — NE klaida. Kartotinis magic link panaudojimas // turi parodyti TA PATI profili, o ne sukurti nauja. if ( 'claimed' === $row->status ) { return array( 'ok' => false, 'reason' => 'already_claimed', 'row' => $row, 'attempt_id' => '' ); } if ( strtotime( $row->expires_at ) < time() ) { return array( 'ok' => false, 'reason' => 'expired', 'row' => $row, 'attempt_id' => '' ); } // SVETIMAS DRAFTAS. Prisi","complete_claim":"function complete_claim( $draft_id, $attempt_id, $user_id, $pet_id ) { global $wpdb; $now = current_time( 'mysql', true ); return (int) $wpdb->query( $wpdb->prepare( 'UPDATE ' . self::table() . \" SET status = 'claimed', payload_json = NULL, claim_started_at = NULL, claim_attempt_id = NULL, claimed_at = %s, claimed_user_id = %d, claimed_pet_id = %d, updated_at = %s WHERE draft_id = %s AND status = 'claiming' AND claim_attempt_id = %s\", $now, (int) $user_id, (int) $pet_id, $now, (string) $draft_id, (string) $attempt_id ) ); } /** * ZINGSNIS 4. Augintinio sukurti nepavyko — draftas GRAZINAMAS i apyvarta. * Be sito zmogus prarastu anketa d","abort_claim":"function abort_claim( $draft_id, $attempt_id ) { global $wpdb; $now = current_time( 'mysql', true ); return (int) $wpdb->query( $wpdb->prepare( 'UPDATE ' . self::table() . \" SET status = 'active', claim_started_at = NULL, claim_attempt_id = NULL, updated_at = %s WHERE draft_id = %s AND status = 'claiming' AND claim_attempt_id = %s\", $now, (string) $draft_id, (string) $attempt_id ) ); } /** * S338: TERMINALI claim pabaiga BE sekmes. * * Naudojama tik toms klaidoms, kuriu PAKARTOJIMAS NIEKO NEISSPRESTU * (pvz. `client_ref_conflict` — draftas priklauso siam vartotojui, bet * augintinis su tuo paciu client_ref priklauso KITAM). Grazinti drafta ","terminate_claim":"function terminate_claim( $draft_id, $attempt_id, $busena = 'conflict' ) { global $wpdb; $now = current_time( 'mysql', true ); return (int) $wpdb->query( $wpdb->prepare( 'UPDATE ' . self::table() . \" SET status = %s, payload_json = NULL, claim_started_at = NULL, claim_attempt_id = NULL, updated_at = %s WHERE draft_id = %s AND status = 'claiming' AND claim_attempt_id = %s\", $busena, $now, (string) $draft_id, (string) $attempt_id ) ); } /** * Crash recovery: augintinis jau sukurtas, bet procesas nulužo pries * complete_claim. Naudojama gavus duplicate-key — tada tai ne klaida. */ public static function find_pet_by_draft( $draft_id ) { global ","recover_stale_claims":"function recover_stale_claims() { global $wpdb; $now = current_time( 'mysql', true ); $riba = gmdate( 'Y-m-d H:i:s', time() - self::CLAIM_STALE_SECONDS ); return (int) $wpdb->query( $wpdb->prepare( 'UPDATE ' . self::table() . \" SET status = 'active', claim_attempt_id = NULL, claim_started_at = NULL, updated_at = %s WHERE status = 'claiming' AND claim_started_at < %s AND expires_at >= %s\", $now, $riba, $now ) ); } public static function stats() { global $wpdb; return $wpdb->get_results( 'SELECT status, COUNT(*) AS c FROM ' . self::table() . ' GROUP BY status', ARRAY_A ); } } "},"drafts_busenos":["claiming","claimed","expired"],"mu_claim":"<?php /** * Petshop Pet Claim Resolve v1.1 (S357/S359) * * duplicate_candidate sprendimas SERVERINIO drafto kelyje (S328/S338). * Po magic link claim'o, radus galima dublikata, draftas grizta i `active`, * o vartotojui irasoma `_ps_pet_claim_pending`. Sis failas duoda: * GET /petshop/v1/pet-claim-pending — ar yra neatsakytas klausimas + duomenys ekranui * POST /petshop/v1/pet-claim-resolve — sprendimas: new (kitas augintinis) / attach (tas pats) * * APSAUGA #3: antros augintinio rasymo logikos NEKURIAM — kvieciami TIK * kanoniniai metodai: Petshop_Pet_Drafts::begin_claim/complete_claim/abort_claim * ir Petshop_Pet_Profile::create_pet_result(). `attach` atveju duomenu merge * atlieka KLIENTAS per esama POST /pet-profile (S207 kelias) — cia tik claim * uzbaigimas. */ if ( ! defined( 'ABSPATH' ) ) exit; const PS_CLAIM_PENDING_META = '_ps_pet_claim_pending'; add_action( 'rest_api_init', function () { register_rest_route( 'petshop/v1', '/pet-claim-pending', array( 'methods' => 'GET', 'callback' => 'ps_pet_claim_pending_get', 'permission_callback' => function () { return is_user_logged_in(); }, ) ); register_rest_route( 'petshop/v1', '/pet-claim-resolve', array( 'methods' => 'POST', 'callback' => 'ps_pet_claim_resolve', 'permission_callback' => function () { return is_user_logged_in(); }, ) ); } ); /** Perskaito pending meta; pasibaigusia arba sugadinta — istrina ir grazina null. */ function ps_pet_claim_meta( $uid ) { $m = get_user_meta( $uid, PS_CLAIM_PENDING_META, true ); if ( is_string( $m ) && '' !== $m ) { $d = json_decode( $m, true ); if ( is_array( $d ) ) { $m = $d; } } if ( ! is_array( $m ) || empty( $m['draft_id'] ) ) { return null; } if ( ! empty( $m['expires","drafts_db":[{"status":"active","n":"7"},{"status":"claimed","n":"4"}],"tokens_db":[{"n":"117"}],"tokens_cols":["id","token_hash","nonce","purpose","purpose_group","subject_id","subject_email","resource_id","action","key_id","status","created_at","expires_at","used_at","used_ip"],"tokens_tipai":[{"t":null,"n":"47"},{"t":"account","n":"15"},{"t":"feedback","n":"16"},{"t":"pet","n":"39"}]}}