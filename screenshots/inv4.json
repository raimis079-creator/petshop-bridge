{
 "v": "INV4",
 "sukurta": 3715,
 "R": {
  "v": "INV4",
  "pdf_dydis": 12190,
  "pdf_metodai": [
   "\tpublic function __construct() {\r",
   "\tpublic function init() {\r",
   "\tpublic function prefetch_locale_fonts() {\r",
   "\tpublic function delete_pdf_files() {\r",
   "\tpublic function generate( $order_id, $template, $data = array(), $force = true ) {\r",
   "\tprotected function get_filename( $order_id, $template ) {\r",
   "\tprotected function get_secure_token( $order_id, $template, $order = null ) {\r",
   "\tpublic function get_url( $order_id, $template ) {\r",
   "\tpublic function delete( $order_id, $template ) {\r",
   "\tprotected function protect_directory( $dir ) {\r"
  ],
  "pdf_ns": "namespace Tyche\\WCDN\\Services;",
  "pdf_ka_daro": [
   " * @package     WCDN/Services\r",
   "\t\t\t$folder = trailingslashit( $upload_path ) . 'wcdn/' . $template;\r",
   "\t\t$base_dir   = trailingslashit( $upload_dir['basedir'] ) . 'wcdn/' . $template . '/';\r",
   "\t\t$upload_fonts_dir = trailingslashit( wp_upload_dir()['basedir'] ) . 'wcdn/fonts';\r",
   "\t\t$options = new Options();\r",
   "\t\t$options->set( 'isRemoteEnabled', true );\r",
   "\t\t$options->set( 'isHtml5ParserEnabled', true );\r",
   "\t\t$options->set( 'isFontSubsettingEnabled', true );\r",
   "\t\t$options->set( 'dpi', apply_filters( 'wcdn_pdf_dpi', 150 ) );\r",
   "\t\t * Use uploads/wcdn/fonts/ as the font cache directory so locale fonts\r",
   "\t\t$options->set( 'fontDir', $upload_fonts_dir );\r",
   "\t\t$options->set( 'fontCache', $upload_fonts_dir );\r",
   "\t\t$options->set(\r",
   "\t\t\t$path = $inter_dir . $font_file;\r",
   "\t\t\tif ( file_exists( $path ) ) {\r",
   "\t\t\t\t\t$path\r",
   "\t\t$base_url   = trailingslashit( $upload_dir['baseurl'] ) . 'wcdn/' . $template . '/';\r",
   "\t\t$base_dir   = trailingslashit( $upload_dir['basedir'] ) . 'wcdn/';\r"
  ],
  "frontend_pdf": [
   "\t\tadd_action( 'wp_ajax_print_order', array( $this, 'template_redirect_admin' ) );\r",
   "\t\tadd_action( 'wp_ajax_wcdn_generate_bulk_pdf', array( $this, 'ajax_generate_bulk_pdf' ) );\r",
   "\t\tadd_action( 'woocommerce_checkout_create_order', array( $this, 'generate_guest_access_token' ), 10, 1 );\r",
   "\t\tadd_action( 'woocommerce_store_api_checkout_update_order_meta', array( $this, 'generate_guest_access_token' ), 10, 1 );\r",
   "\t * Used on the front-end to generate\r",
   "\t\t// PDF is generated synchronously on load for single orders only.\r",
   "\t\t\t$pdf_file = Service::pdf()->generate(\r",
   "\t\t\tif ( $pdf_file ) {\r",
   "\t\t\t\t\t. basename( $pdf_file );\r",
   "\t\t\t\t\t\taction: 'wcdn_generate_bulk_pdf',\r",
   "\t * AJAX handler: generate a merged PDF for multiple orders on demand.\r",
   "\tpublic function ajax_generate_bulk_pdf() {\r",
   "\t\t\twp_send_json_error( array( 'message' => __( 'Authentication failed.', 'woocommerce-delivery-notes' ) ) );\r",
   "\t\t\twp_send_json_error( array( 'message' => __( 'Invalid template.', 'woocommerce-delivery-notes' ) ) );\r",
   "\t\t\twp_send_json_error( array( 'message' => __( 'No orders provided.', 'woocommerce-delivery-notes' ) ) );\r",
   "\t\t\twp_send_json_error( array( 'message' => __( 'No valid orders found.', 'woocommerce-delivery-notes' ) ) );\r",
   "\t\t$pdf_file = Service::pdf()->generate( $order_ids, $template, $documents );\r",
   "\t\tif ( ! $pdf_file ) {\r",
   "\t\t\twp_send_json_error( array( 'message' => __( 'PDF generation failed. Please try with fewer orders or check your server memory limit.', 'woocommerce-delivery-notes' ) ) );\r",
   "\t\t$pdf_url    = trailingslashit( $upload_dir['baseurl'] ) . 'wcdn/' . $template . '/' . basename( $pdf_file );\r",
   "\t\twp_send_json_success( array( 'url' => $pdf_url ) );\r",
   "\t * Generate Guest Access Token.\r",
   "\t * Generates and stores a unique access token for guest users on the order.\r",
   "\t * The token is only generated if the current user is not logged in.\r",
   "\tpublic function generate_guest_access_token( $order ) {\r",
   "\t\t\t\t$token = bin2hex( random_bytes( 16 ) ); // Generate a random token for the guest user.\r"
  ],
  "templates_logo": [
   "\t\t$logo      = $settings['storeLogo'] ?? '';\r",
   "\t\t$logo_path = '';\r",
   "\t\tif ( is_numeric( $logo ) ) {\r",
   "\t\t\t$attached  = get_attached_file( (int) $logo );\r",
   "\t\t\t$logo_path = $attached ? $attached : '';\r",
   "\t\t\t$logo      = wp_get_attachment_url( $logo );\r",
   "\t\t} elseif ( ! empty( $logo ) ) {\r",
   "\t\t\t// storeLogo stored as URL — legacy migration format. Resolve back to\r",
   "\t\t\t$attachment_id = attachment_url_to_postid( $logo );\r",
   "\t\t\t\t$logo_path = $attached ? $attached : '';\r",
   "\t\t * email, logo) without copying base.php, or to inject extra keys your custom\r",
   "\t\t *   @type string $logo       Logo URL (used in HTML context).\r",
   "\t\t *   @type string $logo_path  Logo absolute file path (used in PDF context).\r",
   "\t\t\t\t'logo'      => $logo,\r",
   "\t\t\t\t'logo_path' => $logo_path,\r"
  ],
  "renderer_logo": []
 },
 "isjungta": 3715
}