{
 "deactivated": [],
 "sid": 1940,
 "dry": {
  "schema": {
   "$schema": "http://json-schema.org/draft-07/schema#",
   "title": "cart_abandoned",
   "description": "Krepselis apleistas su identifikuotu email. Reikalauja atskiro cart-tracking modulio (ne vien WC session).",
   "schema_version": 1,
   "type": "object",
   "required": [
    "schema_version",
    "event_name",
    "event_id",
    "emitted_at",
    "email",
    "payload"
   ],
   "properties": {
    "schema_version": {
     "const": 1
    },
    "event_name": {
     "const": "cart_abandoned"
    },
    "event_id": {
     "type": "string"
    },
    "emitted_at": {
     "type": "string",
     "format": "date-time"
    },
    "email": {
     "type": "string",
     "format": "email"
    },
    "customer_id": {
     "type": [
      "integer",
      "null"
     ]
    },
    "payload": {
     "type": "object",
     "required": [
      "cart_id",
      "cart_total",
      "cart_items",
      "last_activity_at"
     ],
     "properties": {
      "cart_id": {
       "type": "string"
      },
      "cart_total": {
       "type": "number"
      },
      "cart_currency": {
       "type": "string",
       "enum": [
        "EUR"
       ]
      },
      "cart_items": {
       "type": "array",
       "items": {
        "type": "object",
        "required": [
         "product_id",
         "name",
         "quantity"
        ],
        "properties": {
         "product_id": {
          "type": "integer"
         },
         "name": {
          "type": "string"
         },
         "quantity": {
          "type": "number"
         },
         "line_total": {
          "type": "number"
         }
        }
       }
      },
      "last_activity_at": {
       "type": "string",
       "format": "date-time"
      },
      "recovery_url": {
       "type": [
        "string",
        "null"
       ]
      }
     }
    }
   }
  },
  "emit_sig": "$event_name, $email, $payload, $opts=opt",
  "emit_head": "\tpublic static function emit( $event_name, $email, array $payload, array $opts = array() ) {\n\t\t$validation = self::validate( $event_name, $payload );\n\n\t\t$strict = isset( $opts['strict'] ) ? (bool) $opts['strict'] : false;\n\t\tif ( ! $validation['valid'] && $strict ) {\n\t\t\treturn array(\n\t\t\t\t'ok'         => false,\n\t\t\t\t'error'      => 'validation_failed',\n\t\t\t\t'validation' => $validation,\n\t\t\t);\n\t\t}\n\t\tif ( ! $validation['valid'] && defined( 'WP_DEBUG' ) && WP_DEBUG ) {\n\t\t\terror_log( '[Petshop Event Registry] ' . $event_name . ' validation: ' . implode( ', ', $validation['errors'] ) );\n\t\t}\n\n\t\t$event_id = isset( $opts['event_id'] ) ? $opts['event_id'] : ( $event_name . '_' . time() . '_' . substr( md5( wp_json_encode( $payload ) ), 0, 8 ) );\n\n\t\t$result = ps_emit_event( $event_id, $event_name, $email, $payload );\n\t\t$result['validation'] = $validation;\n\t\treturn $result;\n\t}\n",
  "carts_before": [
   {
    "cart_id": "c_persist_test",
    "email": null,
    "status": "active"
   }
  ],
  "deleted": 1,
  "carts_after": 0,
  "hooks": {
   "woocommerce_checkout_order_processed": 1,
   "woocommerce_store_api_checkout_order_processed": 1,
   "woocommerce_new_order": 1
  }
 },
 "apply": {
  "schema": {
   "$schema": "http://json-schema.org/draft-07/schema#",
   "title": "cart_abandoned",
   "description": "Krepselis apleistas su identifikuotu email. Reikalauja atskiro cart-tracking modulio (ne vien WC session).",
   "schema_version": 1,
   "type": "object",
   "required": [
    "schema_version",
    "event_name",
    "event_id",
    "emitted_at",
    "email",
    "payload"
   ],
   "properties": {
    "schema_version": {
     "const": 1
    },
    "event_name": {
     "const": "cart_abandoned"
    },
    "event_id": {
     "type": "string"
    },
    "emitted_at": {
     "type": "string",
     "format": "date-time"
    },
    "email": {
     "type": "string",
     "format": "email"
    },
    "customer_id": {
     "type": [
      "integer",
      "null"
     ]
    },
    "payload": {
     "type": "object",
     "required": [
      "cart_id",
      "cart_total",
      "cart_items",
      "last_activity_at"
     ],
     "properties": {
      "cart_id": {
       "type": "string"
      },
      "cart_total": {
       "type": "number"
      },
      "cart_currency": {
       "type": "string",
       "enum": [
        "EUR"
       ]
      },
      "cart_items": {
       "type": "array",
       "items": {
        "type": "object",
        "required": [
         "product_id",
         "name",
         "quantity"
        ],
        "properties": {
         "product_id": {
          "type": "integer"
         },
         "name": {
          "type": "string"
         },
         "quantity": {
          "type": "number"
         },
         "line_total": {
          "type": "number"
         }
        }
       }
      },
      "last_activity_at": {
       "type": "string",
       "format": "date-time"
      },
      "recovery_url": {
       "type": [
        "string",
        "null"
       ]
      }
     }
    }
   }
  },
  "emit_sig": "$event_name, $email, $payload, $opts=opt",
  "emit_head": "\tpublic static function emit( $event_name, $email, array $payload, array $opts = array() ) {\n\t\t$validation = self::validate( $event_name, $payload );\n\n\t\t$strict = isset( $opts['strict'] ) ? (bool) $opts['strict'] : false;\n\t\tif ( ! $validation['valid'] && $strict ) {\n\t\t\treturn array(\n\t\t\t\t'ok'         => false,\n\t\t\t\t'error'      => 'validation_failed',\n\t\t\t\t'validation' => $validation,\n\t\t\t);\n\t\t}\n\t\tif ( ! $validation['valid'] && defined( 'WP_DEBUG' ) && WP_DEBUG ) {\n\t\t\terror_log( '[Petshop Event Registry] ' . $event_name . ' validation: ' . implode( ', ', $validation['errors'] ) );\n\t\t}\n\n\t\t$event_id = isset( $opts['event_id'] ) ? $opts['event_id'] : ( $event_name . '_' . time() . '_' . substr( md5( wp_json_encode( $payload ) ), 0, 8 ) );\n\n\t\t$result = ps_emit_event( $event_id, $event_name, $email, $payload );\n\t\t$result['validation'] = $validation;\n\t\treturn $result;\n\t}\n",
  "carts_before": [],
  "deleted": 0,
  "carts_after": 0,
  "hooks": {
   "woocommerce_checkout_order_processed": 1,
   "woocommerce_store_api_checkout_order_processed": 1,
   "woocommerce_new_order": 1
  }
 },
 "site_code": "200",
 "account_code": "200"
}