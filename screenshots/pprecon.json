{
 "deaktyvuota_TEMP": [],
 "sid": 2014,
 "rez": {
  "VERSIJA": "petprofile-recon-v1",
  "dydis": 38078,
  "routes": "ble` ADD COLUMN `$col` $def\" );\n\t\t\t}\n\t\t}\n\t}\n\n\tpublic static function init() {\n\t\tadd_action( 'rest_api_init', array( __CLASS__, 'register_routes' ) );\n\t}\n\n\tpublic static function register_routes() {\n\t\tregister_rest_route( 'petshop/v1', '/pet-profile', array(\n\t\t\tarray(\n\t\t\t\t'methods'             => 'POST',\n\t\t\t\t'callback'            => array( __CLASS__, 'handle_save' ),\n\t\t\t\t'permission_callback' => array( __CLASS__, 'require_login' ),\n\t\t\t),\n\t\t\tarray(\n\t\t\t\t'methods'             => 'GET',\n\t\t\t\t'callback'            => array( __CLASS__, 'handle_list' ),\n\t\t\t\t'permission_callback' => array( __CLASS__, 'require_login' ),\n\t\t\t),\n\t\t) );\n\t\t// M8: PATCH + DELETE per pet_id\n\t\tregister_rest_route( 'petshop/v1', '/pet-profile/(?P<id>\\d+)', array(\n\t\t\tarray(\n\t\t\t\t'methods'             => 'PATCH',\n\t\t\t\t'callback'            => array( __CLASS__, 'handle_patch' ),\n\t\t\t\t'permission_callback' => array( __CLASS__, 'require_login' ),\n\t\t\t),\n\t\t\tarray(\n\t\t\t\t'methods'             => 'DELETE',\n\t\t\t\t'callback'            => array( __CLASS__, 'handle_delete' ),\n\t\t\t\t'permission_callback' => array( __CLASS__, 'require_login' ),\n\t\t\t),\n\t\t) );\n\t\t// M8: brendu autocomplete\n\t\tregister_rest_route( 'petshop/v1', '/food-search', array(\n\t\t\t'methods'             => 'GET',\n\t\t\t'callback'            => array( __CLASS__, 'handle_food_search' ),\n\t\t\t'permission_callback' => '__return_true',\n\t\t) );\n\n\t\tregister_rest_route( 'petshop/v1', '/brands', array(\n\t\t\t'methods'             => 'GET',\n\t\t\t'callback'            => array( __CLASS__, 'handle_brands' ),\n\t\t\t'permission_callback' => '__return_true',\n\t\t) );\n\t}\n\n\tpublic static function require_login() {\n\t\treturn is_user_logged_in();\n\t}\n\n\tpublic static function handle_save( $request ) {\n\t\t$user_id = get_current_user_id();\n\t\tif ( ! $user_id ) {\n\t\t\treturn new WP_Error( 'not_logged_in', 'Reikia prisijungti.', array( 'status' => 401 ) );\n\t\t}\n\t\t$pet_id = (int) $request->get_param( 'pet_id' );\n\t\tif ( $pet_id > 0 ) {\n\t\t\t$input = self::sanitize_input( $request, true );\n\t\t\treturn self::update_pet( $user_id, $pet_id, $input );\n\t\t}\n\t\t$input = self::sanitize_input( $request, false );\n\t\t$client_ref = self::extract_client_ref( $request );\n\t\t$force_new = (bool) $request->get_param( 'force_new",
  "metodai": [
   "table_name()",
   "install()",
   "maybe_install()",
   "ensure_columns()",
   "init()",
   "register_routes()",
   "require_login()",
   "handle_save($request)",
   "handle_patch($request)",
   "handle_delete($request)",
   "handle_food_search($request)",
   "tax_names($pid, $tax)",
   "grain_flag($pid)",
   "mono_flag($pid)",
   "food_search_title_where($where)",
   "products_cover_weight($product_ids, $weight_kg)",
   "handle_brands($request)",
   "sanitize_input($request, $partial = false)",
   "extract_client_ref($request)",
   "validate_enum($value, $allowed)",
   "create_pet($user_id, $input, $client_ref = null, $force_new = false)",
   "update_pet($user_id, $pet_id, $input)",
   "handle_list($request)",
   "get_pet($pet_id)",
   "normalize_pet_name($name)",
   "find_duplicate_candidates($user_id, $pet_name, $species)",
   "emit_created($user_id, $pet_id, $input)",
   "emit_updated($user_id, $pet_id, $changed)",
   "mirror_to_sender($user_id, $pet_id)"
  ],
  "handleris_vardas": "function create",
  "handleris": "function create_pet( $user_id, $input, $client_ref = null, $force_new = false ) {\n\t\tglobal $wpdb;\n\t\t$now = gmdate( 'Y-m-d H:i:s' );\n\n\t\t// Idempotentiskumas: jei toks client_ref jau perkeltas siam vartotojui -> grazinam esama\n\t\t// (pakartotinis magic link paspaudimas nekuria dublikato).\n\t\tif ( $client_ref ) {\n\t\t\t$existing_id = (int) $wpdb->get_var( $wpdb->prepare(\n\t\t\t\t\"SELECT id FROM `\" . self::table_name() . \"` WHERE user_id = %d AND client_ref = %s AND status='active' LIMIT 1\",\n\t\t\t\t$user_id, $client_ref\n\t\t\t) );\n\t\t\tif ( $existing_id ) {\n\t\t\t\treturn rest_ensure_response( array(\n\t\t\t\t\t'ok'          => true,\n\t\t\t\t\t'pet_id'      => $existing_id,\n\t\t\t\t\t'deduplicated'=> true,\n\t\t\t\t\t'pet'         => self::get_pet( $existing_id ),\n\t\t\t\t) );\n\t\t\t}\n\t\t}\n\n\t\t// S206: semantinis dublikatas. client_ref apsaugo tik nuo TO PATIES juodrascio —\n\t\t// tas pats augintinis is kito irenginio turi klausti vartotojo, ne atsirasti tyliai.\n\t\tif ( ! $force_new && ! empty( $input['pet_name'] ) ) {\n\t\t\t$candidates = self::find_duplicate_candidates(\n\t\t\t\t$user_id,\n\t\t\t\t$input['pet_name'],\n\t\t\t\tisset( $input['species'] ) ? $input['species'] : null\n\t\t\t);\n\t\t\tif ( ! empty( $candidates ) ) {\n\t\t\t\treturn new WP_REST_Response( array(\n\t\t\t\t\t'ok'                   => false,\n\t\t\t\t\t'code'                 => 'duplicate_candidate',\n\t\t\t\t\t'message'              => 'Rastas galimas dublikatas — reikia vartotojo sprendimo.',\n\t\t\t\t\t'duplicate_candidates' => $candidates,\n\t\t\t\t), 409 );\n\t\t\t}\n\t\t}\n\n\t\t$existing_count = (int) $wpdb->get_var( $wpdb->prepare(\n\t\t\t\"SELECT COUNT(*) FROM `\" . self::table_name() . \"` WHERE user_id = %d AND status='active'\",\n\t\t\t$user_id\n\t\t) );\n\t\t$is_primary = ( $existing_count === 0 ) ? 1 : 0;\n\n\t\t$data = array_merge( array(\n\t\t\t'user_id'    => $user_id,\n\t\t\t'is_primary' => $is_primary,\n\t\t\t'status'     => 'active',\n\t\t\t'client_ref' => $client_ref,\n\t\t\t'created_at' => $now,\n\t\t\t'updated_at' => $now,\n\t\t), $input );\n\n\t\t$wpdb->insert( self::table_name(), $data );\n\t\t$pet_id = (int) $wpdb->insert_id;\n\n\t\tif ( ! $pet_id ) {\n\t\t\treturn new WP_Error( 'create_failed', 'Nepavyko išsaugoti anketos.', array( 'status' => 500 ) );\n\t\t}\n\n\t\t// S205: upsert PIRMA, event'as PO — subscriber turi egzistuoti pries atkeliaujant event'ui.\n\t\tif ( $is_primary ) {\n\t\t\tself::mirror_to_sender( $user_id, $pet_id );\n\t\t}\n\t\tself::emit_created( $user_id, $pet_id, $input );\n\n\t\treturn rest_ensure_response( array(\n\t\t\t'ok'         => true,\n\t\t\t'pet_id'     => $pet_id,\n\t\t\t'is_primary' => (bool) $is_primary,\n\t\t\t'pet'        => self::get_pet( $pet_id ),\n\t\t) );\n\t}\n\n\tprivate static function update_pet( $user_id, $pet_id, $input ) {\n\t\tglobal $wpdb;\n\n\t\t$pet = $wpdb->get_row( $wpdb->prepare(\n\t\t\t\"SELECT * FROM `\" . self::table_name() . \"` WHERE id = %d AND user_id = %d AND status!='deleted'\",\n\t\t\t$pet_id, $user_id\n\t\t) );\n\t\tif ( ! $pet ) {\n\t\t\treturn new WP_Error( 'not_found', 'Augintinis nerastas.', array( 'status' => 404 ) );\n\t\t}\n\n\t\t$changed = array();\n\t\t$updates = array();\n\t\tforeach ( $input as $f => $val ) {\n\t\t\tif ( property_exists( $pet, $f",
  "sanitize": "function sanitize_input( $request, $partial = false ) {\n\t\t$out = array();\n\t\t$fields = array(\n\t\t\t'species', 'species_detail', 'pet_name', 'life_stage', 'dog_size',\n\t\t\t'is_sterilised', 'feeding_type', 'feeding_type_other', 'wet_food_g', 'wet_product_id',\n\t\t\t'primary_need', 'primary_need_other', 'current_food_brand',\n\t\t\t'birth_date', 'sensitivities', 'housing', 'activity_hint',\n\t\t\t'current_food_free_text', 'primary_product_id',\n\t\t\t// S211: produkto tapatybe (snapshot)\n\t\t\t'primary_product_sku', 'primary_product_name', 'primary_product_package',\n\t\t);\n\n\t\tforeach ( $fields as $f ) {\n\t\t\t$raw = $request->get_param( $f );\n\t\t\tif ( $partial && $raw === null ) {\n\t\t\t\tcontinue;\n\t\t\t}\n\n\t\t\tswitch ( $f ) {\n\t\t\t\tcase 'species':\n\t\t\t\t\t$v = sanitize_text_field( (string) $raw );\n\t\t\t\t\tif ( in_array( $v, self::SPECIES, true ) ) {\n\t\t\t\t\t\t$out['species'] = $v;\n\t\t\t\t\t} elseif ( ! $partial ) {\n\t\t\t\t\t\t$out['species'] = 'other';\n\t\t\t\t\t}\n\t\t\t\t\tbreak;\n\t\t\t\tcase 'pet_name':\n\t\t\t\t\t$out['pet_name'] = ( $raw !== null && $raw !== '' ) ? sanitize_text_field( (string) $raw ) : null;\n\t\t\t\t\tbreak;\n\t\t\t\tcase 'species_detail':\n\t\t\t\t\t$out['species_detail'] = ( $raw !== null && $raw !== '' ) ? sanitize_text_field( (string) $raw ) : null;\n\t\t\t\t\tbreak;\n\t\t\t\tcase 'current_food_brand':\n\t\t\t\t\t$out['current_food_brand'] = ( $raw !== null && $raw !== '' ) ? sanitize_text_field( (string) $raw ) : null;\n\t\t\t\t\tbreak;\n\t\t\t\tcase 'current_food_free_text':\n\t\t\t\t\t$out['current_food_free_text'] = ( $raw !== null && $raw !== '' ) ? sanitize_text_field( (string) $raw ) : null;\n\t\t\t\t\tbreak;\n\t\t\t\tcase 'primary_product_id':\n\t\t\t\t\t// S246: keiciant produkta SERVERIS pats persirašo snapshot'ą.\n\t\t\t\t\t// Anksciau primary_product_name/_sku/_package buvo leidziamu laukų\n\t\t\t\t\t// sąraše, bet sanitize jiems neturėjo case — todėl tyliai\n\t\t\t\t\t// nukrisdavo: ID pasikeisdavo, o profilyje ir Mityboje likdavo\n\t\t\t\t\t// SENAS pavadinimas. Dabar snapshot'as visada suderintas su ID.\n\t\t\t\t\t$ppid = $raw ? (int) $raw : null;\n\t\t\t\t\t$out['primary_product_id'] = $ppid;\n\t\t\t\t\tif ( $ppid && function_exists( 'wc_get_product' ) ) {\n\t\t\t\t\t\t$pobj = wc_get_product( $ppid );\n\t\t\t\t\t\tif ( $pobj ) {\n\t\t\t\t\t\t\t$parent = $pobj->is_type( 'variation' ) ? (int) $pobj->get_parent_id() : $ppid;\n\t\t\t\t\t\t\t$pak = wp_get_object_terms( $ppid, 'pa_pakuotes_dydis', array( 'fields' => 'names' ) );\n\t\t\t\t\t\t\tif ( is_wp_error( $pak ) || ! $pak ) {\n\t\t\t\t\t\t\t\t$pak = wp_get_object_terms( $parent, 'pa_pakuotes_dydis', array( 'fields' => 'names' ) );\n\t\t\t\t\t\t\t}\n\t\t\t\t\t\t\t$out['primary_product_name']    = mb_substr( (string) $pobj->get_name(), 0, 255 );\n\t\t\t\t\t\t\t$out['primary_product_sku']     = mb_substr( (strin",
  "do_action": [],
  "ps_emit": [],
  "pet_event_vardai": [
   "pet_profile_created",
   "pet_profile_updated"
  ],
  "event_log_stulpeliai": [
   "id",
   "event_id",
   "event_name",
   "email",
   "payload_json",
   "emitted_at",
   "status",
   "adapter_name",
   "attempts",
   "next_retry_at",
   "last_error",
   "esp_response"
  ],
  "event_log_vardai": [
   {
    "event_name": "pet_profile_created",
    "c": "82"
   },
   {
    "event_name": "pet_profile_updated",
    "c": "82"
   },
   {
    "event_name": "order_paid",
    "c": "34"
   },
   {
    "event_name": "consent_changed",
    "c": "24"
   },
   {
    "event_name": "order_shipped",
    "c": "19"
   },
   {
    "event_name": "cart_abandoned",
    "c": "7"
   },
   {
    "event_name": "pet_product_assigned",
    "c": "4"
   },
   {
    "event_name": "refill_due",
    "c": "3"
   },
   {
    "event_name": "pet_reminder_due",
    "c": "1"
   }
  ]
 },
 "site": "200"
}