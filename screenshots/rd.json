{"d":{"sizes":{"pet-ui":6309,"pet-profile":24915,"pet-form.js":29417},"ui_sukurti":[],"ui_enqueue":[" function body_class( $classes ) {\n\t\tif ( false !== get_query_var( self::ENDPOINT, false ) && self::is_onboarding() ) {\n\t\t\t$classes[] = 'ps-pet-onboarding';\n\t\t}\n\t\treturn $classes;\n\t}\n\n\tpublic static function render_account_page() {\n\t\t// S204: profilio ekranas + atskiras formos host (be dubliuoto #pspet-form ID).\n\t\t// ?action=create — serverio kelias: forma atidaroma IS KARTO, veikia ir be JS mygtuko.\n\t\t$create     = isset( $_GET['action'] ) && sanitize_key( wp_unslash( $_GET['action'] ) ) === 'create';\n\t\t$create_url = esc_url( add_query_arg( 'action', 'create', wc_get_account_endpoint_url( self::ENDPOINT ) ) );\n\n\t\t// S211: onboarding rezime navigacija paslepta -> duodam kelia atgal.\n\t\tif ( self::is_onboarding() ) {\n\t\t\techo '<a class=\"ps-back-to-account\" href=\"' . esc_url( wc_get_page_per","ding() ) {\n\t\t\techo '<a class=\"ps-back-to-account\" href=\"' . esc_url( wc_get_page_permalink( 'myaccount' ) ) . '\">&larr; Grįžti į paskyrą</a>';\n\t\t}\n\n\t\techo '<div id=\"pspet-profile\" data-create-url=\"' . $create_url . '\"' . ( $create ? ' style=\"display:none\"' : '' ) . '></div>';\n\t\techo '<div id=\"pspet-form-host\"' . ( $create ? '' : ' style=\"display:none\"' ) . '></div>';\n\t\tif ( $create ) {\n\t\t\techo '<script>window.PS_PET_FORM_OPEN=1;</script>';\n\t\t}\n\t}\n\n\t// --- Shortcode ---\n\tpublic static function shortcode_form( $atts ) {\n\t\tself::enqueue_assets( true ); // force enqueue\n\t\treturn '<div id=\"pspet-form\"></div>';\n\t}\n\n\t// --- Assets ---\n\tpublic static function enqueue_assets( $force = false ) {\n\t\t$base = plugins_url( 'assets/', dirname( __FILE__ ) );\n\n\t\t// S211: paskyros navigacijos stilius —","pspet-form-host\"' . ( $create ? '' : ' style=\"display:none\"' ) . '></div>';\n\t\tif ( $create ) {\n\t\t\techo '<script>window.PS_PET_FORM_OPEN=1;</script>';\n\t\t}\n\t}\n\n\t// --- Shortcode ---\n\tpublic static function shortcode_form( $atts ) {\n\t\tself::enqueue_assets( true ); // force enqueue\n\t\treturn '<div id=\"pspet-form\"></div>';\n\t}\n\n\t// --- Assets ---\n\tpublic static function enqueue_assets( $force = false ) {\n\t\t$base = plugins_url( 'assets/', dirname( __FILE__ ) );\n\n\t\t// S211: paskyros navigacijos stilius — VISUOSE MyAccount puslapiuose,\n\t\t// ne tik augintinio. Kitaip paskyroje butu dvi dizaino sistemos:\n\t\t// \"Mano augintinis\" modernus, o \"Uzsakymai\" — desimt metu atgal.\n\t\tif ( function_exists( 'is_account_page' ) && is_account_page() ) {\n\t\t\twp_enqueue_style( 'pspet-account', $base . 'account.css'","s( 'is_account_page' ) && is_account_page() ) {\n\t\t\twp_enqueue_style( 'pspet-account', $base . 'account.css', array(), self::VERSION );\n\t\t}\n\n\t\t// Iterpiam tik jei puslapyje yra shortcode arba MyAccount augintinis tab\n\t\tif ( ! $force && ! self::should_load() ) {\n\t\t\treturn;\n\t\t}\n\n\t\twp_enqueue_style( 'pspet-form', $base . 'pet-form.css', array(), self::VERSION );\n\t\t// Inter is Google Fonts\n\t\twp_enqueue_style( 'pspet-inter', 'https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap', array(), null );\n\n\t\twp_enqueue_script( 'pspet-form', $base . 'pet-form.js', array(), self::VERSION, true );\n\n\t\t// Profilio ekranas — MyAccount puslapyje (JS pats tikrina ar yra #pspet-profile).\n\t\t// S204: papildomas saugiklis — endpoint query var (jei is_account_page() del temos/konteksto nes"],"ui_enqueue2":["s' ) );\n\t\tadd_action( 'woocommerce_account_' . self::ENDPOINT . '_endpoint', array( __CLASS__, 'render_account_page' ) );\n\n\t\t// Assets\n\t\tadd_action( 'wp_enqueue_scripts', array( __CLASS__, 'enqueue_assets' ) );\n\t}\n\n\t// --- MyAccount endpoint ---\n\tpublic static function add_endpoint() {\n\t\tadd_rewrite_endpoint( self::ENDPOINT, EP_ROOT | EP_PAGES );\n\t}\n\n\tpublic static function add_menu_item( $items ) {\n\t\t// Idedam \"Mano augintinis\" po Dashboard (pirmas), pries Orders\n\t\t$new = array();\n\t\tforeach ( $","s Google Fonts\n\t\twp_enqueue_style( 'pspet-inter', 'https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap', array(), null );\n\n\t\twp_enqueue_script( 'pspet-form', $base . 'pet-form.js', array(), self::VERSION, true );\n\n\t\t// Profilio ekranas — MyAccount puslapyje (JS pats tikrina ar yra #pspet-profile).\n\t\t// S204: papildomas saugiklis — endpoint query var (jei is_account_page() del temos/konteksto nesuveiktu).\n\t\tif ( ( function_exists( 'is_account_page' ) && is_account_pag","onteksto nesuveiktu).\n\t\tif ( ( function_exists( 'is_account_page' ) && is_account_page() ) || false !== get_query_var( self::ENDPOINT, false ) ) {\n\t\t\twp_enqueue_script( 'pspet-profile', $base . 'pet-profile.js', array(), self::VERSION, true );\n\t\t}\n\n\t\t$config = array(\n\t\t\t'restUrl'     => esc_url_raw( get_rest_url( null, 'petshop/v1' ) ),\n\t\t\t'nonce'       => wp_create_nonce( 'wp_rest' ),\n\t\t\t'isLoggedIn'  => is_user_logged_in(),\n\t\t\t'homeUrl'     => home_url( '/' ),\n\t\t\t'petPageUrl'  => wc_get_accoun"],"ui_pspetform":[],"ui_action":["hortcode anketai\n\t\tadd_shortcode( 'petshop_pet_form', array( __CLASS__, 'shortcode_form' ) );\n\n\t\t// MyAccount tab\n\t\tadd_action( 'init', array( __CLASS__, 'add_endpoint' ) );\n\t\tadd_filter( 'woocommerce_account_menu_items', array( __CLASS__, 'add_menu_item' ) );\n\t\tadd_filter( 'body_class', array( __CLASS__, 'body_class' ","nu_items', array( __CLASS__, 'add_menu_item' ) );\n\t\tadd_filter( 'body_class', array( __CLASS__, 'body_class' ) );\n\t\tadd_action( 'woocommerce_account_' . self::ENDPOINT . '_endpoint', array( __CLASS__, 'render_account_page' ) );\n\n\t\t// Assets\n\t\tadd_action( 'wp_enqueue_scripts', array( __CLASS__, 'enqueue_assets' ) );\n\t}\n"," 'woocommerce_account_' . self::ENDPOINT . '_endpoint', array( __CLASS__, 'render_account_page' ) );\n\n\t\t// Assets\n\t\tadd_action( 'wp_enqueue_scripts', array( __CLASS__, 'enqueue_assets' ) );\n\t}\n\n\t// --- MyAccount endpoint ---\n\tpublic static function add_endpoint() {\n\t\tadd_rewrite_endpoint( self::ENDPOINT, EP_ROOT | EP_P","\tprivate static function is_onboarding() {\n\t\tif ( ! is_user_logged_in() ) {\n\t\t\treturn false;\n\t\t}\n\t\tif ( ! isset( $_GET['action'] ) || sanitize_key( wp_unslash( $_GET['action'] ) ) !== 'create' ) {\n\t\t\treturn false;\n\t\t}\n\t\tglobal $wpdb;\n\t\t$count = (int) $wpdb->get_var( $wpdb->prepare(\n\t\t\t\"SELECT COUNT(*) FROM `{$wpdb->pre"],"js_init":["\tshowDraftPrompt(draft);\n\t\t\t\tPetshopPetForm.mounted = true;\n\t\t\t\treturn true;\n\t\t\t}\n\t\t}\n\t\trender();\n\t\tPetshopPetForm.mounted = true;\n\t\treturn true;\n\t}\n\n\tvar PetshopPetForm = { mounted: false, mount: mount };\n\twindow.PetshopPetForm = PetshopPetForm;\n\t// Atgalinis suderinamumas su senu kvietimu\n\twindow.PSPetFormInit = function(){\n\t\tvar host = document.getElementById('pspet-form-host') || document.getElementById('pspet-form');\n\t\treturn mount(host);\n\t};\n\n\t// --- Init: auto-mount tik ten, kur forma turi rodytis IS KARTO ---\n\tfunction init(){\n\t\tvar host = document.getElementById('pspet-form-host');\n\t\tvar sc = document.getElementById('pspet-form');\n\t\t// Serverio kelias ?action=create (veikia ir be JS mygtuko)\n\t\tif (window.PS_PET_FORM_OPEN && host) { mount(host); return; }\n\t\t// Shortcode puslapis (anketa matoma is karto)\n\t\tif (sc && !host) { mount(sc); return; }\n\t\t// MyAccount be action=create —"],"js_mount":["et-error', msg);\n\t\troot.insertBefore(e, root.firstChild);\n\t\tsetTimeout(function(){ e.remove(); }, 5000);\n\t}\n\n\t// --- Mount API (S204): forma montuojama i BET KOKI konteineri, ne root.id ---\n\tfunction mount(container, opts){\n\t\tif (!container) return false;\n\t\troot = container;\n\t\tif (root.style && root.style.display === 'none') root.style.display = '';\n\t\troot.innerHTML = '';\n\t\topts = opts || {};\n\n\t\t// S208: kiekvienas mount pradeda nuo SVARIOS busenos. Be sito \"Prideti kita augintini\"\n\t\t// po ka tik sukurto profilio atsivertu su ankstesnio augintinio duomenimis.\n\t\t// opts.data — redagavimas (uz","ts){\n\t\tif (!container) return false;\n\t\troot = container;\n\t\tif (root.style && root.style.display === 'none') root.style.display = '';\n\t\troot.innerHTML = '';\n\t\topts = opts || {};\n\n\t\t// S208: kiekvienas mount pradeda nuo SVARIOS busenos. Be sito \"Prideti kita augintini\"\n\t\t// po ka tik sukurto profilio atsivertu su ankstesnio augintinio duomenimis.\n\t\t// opts.data — redagavimas (uzpildyta anketa), be jo — nauja anketa.\n\t\tstate.step       = opts.step || 1;\n\t\tstate.data       = opts.data ? opts.data : {};\n\t\tstate.editPetId  = opts.petId || null;\n\t\tstate.duplicates = null;\n\t\tstate.savedPetId = nul","tis anoniminis — patikrinam juodraštį\n\t\tif (!IS_LOGGED_IN) {\n\t\t\tvar draft = loadDraft();\n\t\t\tif (draft && draft.pet_data && draft.pet_data.species) {\n\t\t\t\tshowDraftPrompt(draft);\n\t\t\t\tPetshopPetForm.mounted = true;\n\t\t\t\treturn true;\n\t\t\t}\n\t\t}\n\t\trender();\n\t\tPetshopPetForm.mounted = true;\n\t\treturn true;\n\t}\n\n\tvar PetshopPetForm = { mounted: false, mount: mount };\n\twindow.PetshopPetForm = PetshopPetForm;\n\t// Atgalinis suderinamumas su senu kvietimu\n\twindow.PSPetFormInit = function(){\n\t\tvar host = document.getElementById('pspet-form-host') || document.getElementById('pspet-form');\n\t\treturn mount(hos"," draft = loadDraft();\n\t\t\tif (draft && draft.pet_data && draft.pet_data.species) {\n\t\t\t\tshowDraftPrompt(draft);\n\t\t\t\tPetshopPetForm.mounted = true;\n\t\t\t\treturn true;\n\t\t\t}\n\t\t}\n\t\trender();\n\t\tPetshopPetForm.mounted = true;\n\t\treturn true;\n\t}\n\n\tvar PetshopPetForm = { mounted: false, mount: mount };\n\twindow.PetshopPetForm = PetshopPetForm;\n\t// Atgalinis suderinamumas su senu kvietimu\n\twindow.PSPetFormInit = function(){\n\t\tvar host = document.getElementById('pspet-form-host') || document.getElementById('pspet-form');\n\t\treturn mount(host);\n\t};\n\n\t// --- Init: auto-mount tik ten, kur forma turi rodytis IS KA"],"js_rootid":["var e = el('div', 'pspet-error', msg);\n\t\troot.insertBefore(e, root.firstChild);\n\t\tsetTimeout(function(){ e.remove(); }, 5000);\n\t}\n\n\t// --- Mount API (S204): forma montuojama i BET KOKI konteineri, ne root.id ---\n\tfunction mount(container, opts){\n\t\tif (!container) return false;\n\t\troot = container;\n\t\tif (root.style && root.style.display === 'none') root.style.display = '';\n\t\troot.innerHTML = '';\n\t\topts = opts || {};\n\n\t\t// S208: kiekvienas mount pradeda nuo SVARIOS busenos. Be sito \"Prideti kita au"],"js_head":"/* Petshop \"Mano augintinis\" anketa — M8 v1.5 (S206 dublikatai + S208 redagavimas + S211 produkto tapatybe + foto po prisijungimo) */\n(function(){\n\t'use strict';\n\n\tvar CFG = window.PSPetConfig || {};\n\tvar REST = CFG.restUrl || '/wp-json/petshop/v1';\n\tvar IMAGES = CFG.imagesUrl || '';\n\n\tvar SPECIES_KEY = { dog:'pet-dog', cat:'pet-cat', bird:'pet-bird', rodent:'pet-rodent', fish:'pet-fish', reptile:'pet-reptile', other:'pet-other' };\n\n\t// Didele iliustracija (header/avataras) — originalas.\n\tfunction speciesImg(species){\n\t\tvar key = SPECIES_KEY[species] || 'pet-other';\n\t\tif (IMAGES) return '<img src=\"' + IMAGES + key + '.png\" alt=\"\" style=\"width:100%;height:100%;object-fit:contain\">';\n\t\tret","ui_hooks":["ray( __CLASS__, 'shortcode_form' ) );\n\n\t\t// MyAccount tab\n\t\tadd_action( 'init', array( __CLASS__, 'add_endpoint' ) );\n\t\tadd_filter( 'woocommerce_account_menu_items', array( __CLASS","filter( 'body_class', array( __CLASS__, 'body_class' ) );\n\t\tadd_action( 'woocommerce_account_' . self::ENDPOINT . '_endpoint', array( __CLASS__, 'render_account_page' ) );\n\n\t\t// As","array( __CLASS__, 'render_account_page' ) );\n\n\t\t// Assets\n\t\tadd_action( 'wp_enqueue_scripts', array( __CLASS__, 'enqueue_assets' ) );\n\t}\n\n\t// --- MyAccount endpoint ---\n\tpublic sta"]}}